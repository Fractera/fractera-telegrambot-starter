// 🪦 СЛОВАРЬ ПЕРЕЕХАЛ В `lib/i18n/voice-field.i18n.json` (шаг 32-4, 2026-08-28).
//
// Здесь стояли десять языков объектами прямо в коде. Механизм обмена переводами
// (`npm run i18n:export` / `i18n:import`) читает и пишет ФАЙЛ и такой словарь не
// видит вовсе — то есть владелец не мог ни выгрузить массив для внешней модели,
// ни внести её ответ, а именно этого он и просил.
//
// 🔒 ФАЙЛ ОСТАЁТСЯ ТОНКИМ РЕЭКСПОРТОМ, А НЕ УДАЛЯЕТСЯ. По нему инструмент
// зовут из нескольких мест, и оборвать путь одним переездом значило бы чинить
// импорты там, где работа этого шага не идёт. Реэкспорт стоит одну строку и
// живёт до тех пор, пока последний потребитель не перейдёт на новый адрес.

export { voiceStrings, type VoiceStrings } from "@/lib/i18n/voice-field.i18n"
