// Значок микрофона — один на оба контейнера формы (шаг 32-5, 2026-08-28).
//
// 🔒 ВЫНЕСЕН, А НЕ СКОПИРОВАН. Он понадобился второму элементу (`VoiceTextarea`),
// и десять строк SVG выглядят как «дешевле повторить». Дешевле ровно один раз:
// перечёркнутая версия значка (`off`) есть утверждение «микрофон здесь не
// работает», и две копии этого утверждения разойдутся молча — в одном элементе
// перечёркивание останется, в другом при первой же правке пропадёт.
//
// 🔒 У ИНСТРУМЕНТА `_tools/voice-input` ОСТАЁТСЯ СВОЙ ЗНАЧОК, И ЭТО НЕ
// ДУБЛИРОВАНИЕ ПО НЕДОСМОТРУ. Инструмент обязан быть переносимым архивом:
// распаковал папку в другом проекте — и всё работает. Импорт из `components/`
// сделал бы его зависимым от нашей библиотеки; это записано законом в его
// собственной шапке и здесь только повторено как причина, а не как правило.

export function MicIcon({ off }: { off?: boolean }) {
  return (
    <svg
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth="2"
      strokeLinecap="round"
      className="size-4"
      aria-hidden
    >
      <path d="M12 2a3 3 0 0 0-3 3v6a3 3 0 0 0 6 0V5a3 3 0 0 0-3-3z" />
      <path d="M19 10v1a7 7 0 0 1-14 0v-1M12 18v4" />
      {off ? <path d="M3 3l18 18" /> : null}
    </svg>
  )
}
