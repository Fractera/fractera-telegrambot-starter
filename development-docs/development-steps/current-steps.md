> # 🔒 СЛУЖБА 3600 ОТВЯЗАНА ОТ КОНФИГОВ 3000 ПОЛНОСТЬЮ — 2026-09-06, `b05f1e1`, СОБРАНО
>
> **Слово владельца:** «убери оставшиеся две зависимости — палитру и часовой пояс». И его же
> вопрос, на который пришлось ответить до работы: «убери палитру — это значит у меня сайт больше не
> будет красивый зелёный?»
>
> 🔒 **ОТВЕТ БЫЛ «НЕТ», И ОН ОКАЗАЛСЯ ВАЖНЕЕ САМОЙ ПРАВКИ.** «Убрать зависимость» значит перестать
> ОДАЛЖИВАТЬ палитру у чужого проекта, а не убрать палитру. Измерение показало, что верно
> противоположное опасению: собственные умолчания службы — `colors: { light: {}, dark: {} }`, то
> есть **пусто**. Сотри владелец порт 3000 сегодня — зелёный исчез бы, и служба посерела. Правка не
> отнимает цвет, а спасает его.
>
> | Что | Откуда бралось | Где теперь |
> |---|---|---|
> | палитра, 7+7 ролей | `DESIGN_CONFIG_PATH` → файл слота 3000 | `DESIGN-CONFIG/design-config.json` в дереве службы |
> | часовой пояс | `APP_CONFIG_PATH` → файл слота 3000 | `APP-CONFIG/app-config.json` в дереве службы |
>
> Значения перенесены **символ в символ** (сверено программно): `primary` `#16a34a` в светлой,
> `#4ade80` в тёмной. Вид не изменился ни на пиксель.
>
> ## ✗ ПОПУТНЫЙ ДЕФЕКТ, НАЙДЕННЫЙ ИЗМЕРЕНИЕМ, — И ОН БЫЛ МОЛЧАЛИВЫЙ
>
> ```
> читатель  timezoneOf()    → APP_CONFIG_PATH = /opt/fractera/app/APP-CONFIG/...   (порт 3000)
> писатель  saveTimezone()  → process.cwd()/APP-CONFIG/...                          (своё дерево)
>                             ls: No such file or directory
> ```
> Бот спрашивал у человека часовой пояс, **писал ответ в несуществующую папку** и на следующем
> запросе читал чужой файл, где пояса нет. Напоминания оставались по Гринвичу навсегда, и человек
> узнал бы об этом, проспав встречу.
> 🔒 **ЛЕЧЕНИЕ — СОВПАДЕНИЕ АДРЕСОВ, А НЕ ПРАВКА КОДА.** Заведена своя папка, путь из окружения
> снят — оба конца смотрят в один файл. `timezone.ts` не изменился ни строкой.
> **Доказано живьём:** `saveTimezone("Europe/Madrid")` вернул `true`, значение легло в файл службы.
> До правки папки не существовало и вызов возвращал `false`. След пробника убран, файл возвращён к `{}`.
>
> ## Рождение нового сервера правлено тем же движением
>
> `fractera-easy-starter` `ec3884e`: три строки `*_CONFIG_PATH` убраны из `lib/bootstrap.sh` с
> надгробием и причиной. ✗ иначе следующий сервер родился бы с той же привязкой, и правка службы
> ничего бы не значила.
>
> **Доказательства, две плоскости.**
> 1. **Оба источника окружения чисты, и других нет.** В `.env.local` службы остался один путь —
>    `FRACTERA_SLOT_ENV` (секреты). В окружении живого процесса `*_CONFIG_PATH` — **0**; негативный
>    контроль тем же прибором: `PORT` — **1**, значит прибор различает.
> 2. **Читатель идёт по умолчанию в своё дерево.** `cwd` процесса `/opt/fractera/telegrambot`,
>    файл `DESIGN-CONFIG/design-config.json` там лежит и несёт `#16a34a` / `#4ade80`.
>
> 🛑 **ЧЕГО Я НЕ ДОКАЗАЛ И НЕ ВЫДАЮ ЗА ДОКАЗАННОЕ: ЗЕЛЁНЫЙ ГЛАЗАМИ.** Страницы службы закрыты
> ролями и отдают `307` моим приборам; цвет печатается в HTML на запрос, значит в собранных файлах
> его нет. Увидеть его может только владелец, открыв `https://chat.aifa.dev/ru/settings`.
>
> **Сервер:** `HEAD` `b05f1e1`, `BUILD_RC=0`, `TSC_RC=0`, порт 3600 держит потомок pm2
> (`739697` ← `739696`), `/ru/settings` → `307`. Копия прежнего окружения —
> `/opt/fractera/telegrambot/.env.local.bak-20260906`.
>
> **Что осталось от 3000 и почему это другое:** `FRACTERA_SLOT_ENV` — семь файлов читают через него
> **секреты** (ключ слоя данных, ключ OpenAI), а не оформление. Отдельное решение владельца.

---

> # 🔒 ОБОЛОЧКА СЛУЖБЫ СВОЯ · ГЕРМЕС ОТМЕНЁН — 2026-09-06, `b96cd7b`, СОБРАНО
>
> **Слова владельца того дня, дословно:** «никакие другие импорты из слоя 3000 нам не нужны» ·
> «хедер убираем кнопку войти и оставляем только слева кнопку Fractera» · «от футера у нас
> остаётся только надпись год — Fractera, все права защищены» плюс иконки темы · «Гермес это не
> актуально, мы отменяем Гермес, так как наша архитектура будет иметь собственный поток эволюции —
> убери везде, где упоминается».
>
> 🔒 **ЭТО СНЯЛО БÓЛЬШУЮ ЧАСТЬ ОТВЯЗКИ ОДНИМ РЕШЕНИЕМ, А НЕ ПЕРЕНОСОМ КОНФИГОВ.** Я планировал
> заводить службе свои `APP-CONFIG` и `PLATFORM-CONFIG`. Владелец решил иначе: **убрать из шапки и
> подвала всё, что оттуда читалось**. Тогда конфиги 3000 им просто перестают быть нужны — и второй
> копии настроек заводить не надо вовсе. **Дешевле переноса на порядок.**
>
> | | Было | Стало |
> |---|---|---|
> | шапка | `TopMenu`, 115 строк: меню из `APP-CONFIG`, ящики, аккаунт, корзина, мобильное меню | `components/shell/site-header.tsx` — одна ссылка «Fractera» слева |
> | подвал | `FooterMenu`, 481 строка: группы меню, соцсети, ссылки в панель и слой архитектора, куки-баннер, язык, ширина | `site-footer.tsx` — «© год Fractera. Все права защищены.» + переключатель темы |
> | файлов, импортирующих конфиги 3000 | 18 · 13 · 6 совпадений | **2 настоящих импорта** |
>
> **Осталось ровно два, и оба названы:** `lib/design-css.ts` (палитра из `DESIGN-CONFIG`) и
> `lib/products/telegram-desk/timezone.ts` (часовой пояс из `APP-CONFIG`). Плюс `slotEnv` — семь
> файлов, но это **секреты** (ключ слоя данных, ключ OpenAI), а не оформление; их судьба — отдельное
> решение владельца.
>
> **Удалено, восстанавливается из git:** `components/menu/` целиком (21 файл), `lib/menu/`,
> `providers/drawer-provider.client.tsx`. Оставлен один — переключатель темы, переехал в
> `components/shell/theme-toggle.client.tsx`.
>
> 🪦 **ОТМЕНЯЕТ ЗАКАЗ 137-3 «add header and footer 1:1 as in port 3000».** Тогда шапку и подвал
> перенесли побайтно — и вместе с ВИДОМ приехал ИСТОЧНИК. Это тот же класс ошибки, которым оплачен
> весь шаг 137: **«переносим вид» и «переносим источник» — разные работы.**
>
> ## Гермес отменён
>
> `TOOLS-CONFIG` 5 → **4** записи · `REGISTRY-CONFIG` 36 → **35** (`initiator.hermes`) ·
> `INITIATORS` три значения вместо четырёх, блок `INITIATOR_TEXTS.hermes` вырезан ·
> поддомен `hermes` убран из `KNOWN_PREFIXES` · комментарий в `app-config.defaults` обезличен.
>
> 🛑 **В ДРУГИХ РЕПОЗИТОРИЯХ ГЕРМЕС ОСТАЛСЯ, И ЭТО НАЗВАНО, А НЕ ЗАБЫТО:** `fractera-next-starter`
> — 9 файлов, `fractera-easy-starter` — 98, `ai-workspace` — **815**. Там это в основном история
> снесённой подсистемы (`ARCHIVE-500`), и чистка такого объёма — отдельное решение владельца.
> Итоги закрытых шагов здесь тоже не правились: они записывают, что было.
>
> **Доказательства.** Сборка: `BUILD_RC=0`, сторож в её логе — `реестр и код сходятся: 35`.
> В собранном коде `Гермес` → **0** файлов, `hermes` → **0**; контроль `Fractera` → 27 файлов, то
> есть поиск работает. `TopMenu`/`FooterMenu` остались только надгробиями в комментариях (2 строки).
> Порт 3600 держит потомок pm2 (`738612` ← `738611` ← `738594`), `/ru/settings` → `307`, `TSC_RC=0`.

---

> # 🔒 ДВА РЕЕСТРА ЖИВУТ В ВЕРХНЕМ СЛОЕ И ЧИТАЮТСЯ ОТТУДА — 2026-09-06, `26c8322`, СОБРАНО
>
> **Решения владельца того дня, дословно:** «убери это сраное слово факт, должно быть всего два
> слова реестр CONFIG» · «также делай реестр Тулс, и переноси туда все существующие данные — мы
> должны читать и писать их из этих реестров» · «те данные, которые мы рисуем в настройках,
> читались именно отсюда» · «так как их создавать теперь будет искусственный интеллект, оставь
> внутри файл read me и опиши, как искусственный интеллект должен делать эти записи».
>
> | Реестр | Где лежит | Что было до |
> |---|---|---|
> | признаков | `REGISTRY-CONFIG/registry-config.json` — **36 записей** | 36 собирались в коде (`lib/facts/builtin.ts`), добавленные — в таблице `fact_registry` |
> | инструментов | `TOOLS-CONFIG/tools-config.json` — **5 записей** | массив `TOOLS` **внутри компонента экрана** + `lib/task/actors.ts` |
>
> **Удалены:** `lib/facts/builtin.ts` (388 строк), `lib/task/actors.ts` (74). Восстанавливаются из
> git. **Заведены:** `lib/tools/store.ts`, `scripts/check-registry.mjs`. `registry.ts` — 42 строки,
> тонкий посредник: его зовут экран, дверь, `ensure.ts`, `detail.ts`, и ни один не знает, откуда
> берутся записи.
>
> 🔒 **ЗАКОН 81-1 «ВСТРОЕННЫЕ ПОРОЖДАЮТСЯ ИЗ КОДА» ОТМЕНЁН ВЛАДЕЛЬЦЕМ, НО ЕГО ПРИЧИНА ЗАКРЫТА
> СТОРОЖЕМ, А НЕ ЗАБЫТА.** Пять списков (`INITIATORS`, `ARRIVAL_KINDS`, `INTENTS`, `ENTRY_KINDS`,
> `ARTIFACT_KINDS`) остаются в коде, потому что объявлены `as const` и **порождают типы** —
> `Intent`, `EntryKind`, `ArrivalKind`. Перенеси их в JSON, и опечатка в намерении перестанет
> ловиться проверкой типов. `scripts/check-registry.mjs` сверяет списки с реестром в обе стороны и
> роняет сборку, называя недостающий ключ.
>
> 🛑 **СТОРОЖ СТОИТ ПЕРВЫМ ШАГОМ `build`, А НЕ В `prebuild`, И ЭТО ИЗМЕРЕНО.** Здесь **pnpm 10**, а
> он исполняет `pre`/`post`-скрипты только при `enable-pre-post-scripts=true` в `.npmrc`; такой
> строки у проекта нет. В `prebuild` сторож не запускался бы **молча**, и сборка была бы зелёной
> всегда — ровно то, от чего он заводится. **Приём общий: сторож, поставленный в хук, который не
> вызывается, хуже отсутствующего.**
>
> 🔒 **ИСКЛЮЧЕНИЕ У СТОРОЖА НАЗВАНО, А НЕ ПРОЩЕНО:** уровень `field` описывает колонки
> `tgdesk_messages`, а колонка — не список, сверять её не с чем. Долгов ноль, механизм долга
> оставлен пустым списком.
>
> **Доказательства, три плоскости.**
> 1. **Неизменность (рантайм).** До и после — одно и то же: `36 признаков`, уровни
>    `initiator 4 · material 9 · intent 8 · entity 6 · destination 3 · field 6`, обязательный `1`,
>    `storedIn` сохранены (`material.voice → tgdesk_messages.raw_kind`); инструментов `5`,
>    настоящих `3`, порядок `inbox-store → fact-matcher → registry-evolution → link-finder → hermes`.
> 2. **Сторож различает.** Добавил в код намерение `probe-intent`, не описав в реестре →
>    `✗ INTENTS: в коде есть, в реестре НЕТ — intent.probe-intent`, `RC=1`. Убрал → `RC=0`.
> 3. **Собранный код на сервере.** Строка из `TOOLS-CONFIG` → 2 файла, `intent.capture` → 4 файла,
>    заведомо ложная строка → **0**, `fact_registry` → **0**. Вывод сторожа найден **в логе самой
>    сборки** — доказательство, что он запустился, а не просто существует.
>
> **Сервер:** `HEAD` `26c8322`, `BUILD_RC=0`, `TSC_RC=0`, порт 3600 держит потомок pm2
> (`737525` ← `737524` ← pm2 `737509`), `/ru/settings` → `307`.
> 🛑 Единственная строка `Error:` в логе — прежняя `Failed to find Server Action "x"` от вкладки,
> открытой на старой сборке. Была там до этой работы.
>
> **READMEs написаны ДЛЯ ИИ-АВТОРА** (`REGISTRY-CONFIG/README.md`, `TOOLS-CONFIG/README.md`):
> порядок работы по шагам, форма записи, закрытые списки и то, что чужое значение становится
> умолчанием, а не отказом; раздел «чего делать нельзя» с причиной у каждого запрета.
>
> ✗ **ПОПУТНО ОПЛАЧЕНО.** Первую версию сторожа я собрал на регулярном выражении в шаблонной
> строке: `\s` в шаблоне превращается в `s` **молча**, и regexp стал другим regexp. Разбор
> переписан скан-ом по кавычкам — там экранироваться нечему.

---

> # 🔒 ОПРЕДЕЛЕНИЯ ПРИЗНАКОВ ПЕРЕЕХАЛИ ИЗ БАЗЫ В ФАЙЛ 2026-09-06 — `08d00d5`, СОБРАНО
>
> **Слово владельца:** «всё, что в базе данных, — убрать» · «чтобы агент читал его прямо из
> репозитория, без слоя данных».
>
> 🔒 **ЭТО СЛЕДСТВИЕ 137-13, А НЕ ОТДЕЛЬНАЯ РАБОТА, И ПОРЯДОК ЗДЕСЬ ПРИЧИННЫЙ.** Пока реестр
> правился формой на экране, хранилищем ОБЯЗАНА была быть база: форма пишет в работающей системе и
> файла репозитория не видит. Как только 137-13 забрал правку у формы и отдал агенту, писателем
> стал тот, кто правит ФАЙЛЫ и кладёт коммит, — и хранилище обязано было переехать следом.
> Оставить его в базе значило дать определениям двух хозяев.
>
> | Что | Было | Стало |
> |---|---|---|
> | где лежат определения | таблица `fact_registry` слоя данных | `FACT-REGISTRY-CONFIG/fact-registry-config.json`, верхний слой, статический импорт |
> | чтение | `lib/facts/registry.ts`, 274 строки, `async`, через `dataFetch` | `lib/facts/store.ts` + `registry.ts` 50 строк, синхронно |
> | запись | `addFact` · `updateFact` · `disableFact` | **нет вовсе** — пишет агент, правя файл |
> | дверь `api/architect/facts` | 186 строк, GET + 130 строк проверки тела | 42 строки, только GET |
>
> 🔒 **КОНФИГ ЛЕЖИТ В ВЕРХНЕМ СЛОЕ, КАК СОСЕДИ, И ЭТО ИСПРАВЛЕНИЕ МОЕЙ ОШИБКИ ТОГО ЖЕ ДНЯ**
> (`e969e71`). Слово владельца: «почему это не лежит верхний слой так же, как `APP-CONFIG` лежит
> в верхнем слое порт 3000? почему в порт 3600 тычешь CONFIG файл внутри функции на несколько
> слоёв глубину».
> ✗ **Механизм ошибки назван, потому что он повторится:** владелец оставил за собой имя конфига и
> отверг `FACTS-CONFIG`. Вместо того чтобы спросить имя ещё раз, я спрятал файл внутрь
> `lib/facts/`, **где имя не нужно**. Это обход вопроса, а не решение: неудобный вопрос был
> заменён удобной раскладкой.
> 🔒 **Имя взято ИЗ ПРОЕКТА, а не выдумано** — раздел на экране зовётся «Fact registry» /
> «Реестр признаков» (`telegram.i18n.ts:610`, `:1001`).
> 🔒 **Это ПЕРВАЯ собственная конфиг-папка службы:** у 3600 их было ноль, все четыре читались из
> дерева слота 3000. По её образцу лягут остальные при отвязке конфигов.
> 🔒 **`schema.json` и `defaults.json` не заведены намеренно**, в отличие от соседей: у них схема
> кормит форму на экране, здесь формы нет, и вторая копия описания полей разошлась бы с
> `lib/facts/types.ts` молча. Единственный источник формы — тип `Fact`.
>
> **Доказано на новом адресе:** пустой файл → `36, все встроенные, из конфига 0`; одна запись в
> `FACT-REGISTRY-CONFIG` → `37, встроенных 36, из конфига 1`. Значит читается именно он.
> **Сервер:** `HEAD` `e969e71`, `TSC_RC=0`, `BUILD_RC=0`, старого адреса нет
> (`ls lib/facts/registry.json` → `No such file or directory`), порт 3600 держит потомок pm2
> (`736649` ← `736648`, pm2 `736635`), `/ru/settings` → `307`.
>
> 🔒 **ПЕРЕЕХАЛИ ТОЛЬКО ОПРЕДЕЛЕНИЯ.** Значения фактов остаются в базе: их порождают сообщения, их
> много и они растут. `lib/facts/ensure.ts` не тронут и по-прежнему создаёт таблицы признаков.
>
> ✗ **ПОПУТНАЯ НАХОДКА, ИСПРАВЛЕНА ЗДЕСЬ ЖЕ:** 137-13 снял `POST`/`PATCH`, но оставил всю их
> проверку висеть — `readSecondLayer()`, `hostOfFnAllowed()`, три предела, помощник `no()`, 130
> строк, которые никто не звал. **Мёртвый код рядом с живым читается как действующее правило.**
>
> 🔒 **ИЗМЕРЕНО ДО ПЕРЕЕЗДА, А НЕ ПРЕДПОЛОЖЕНО:** `SELECT COUNT(*) FROM fact_registry` = **0**,
> терять было нечего (негативный контроль: ложный секрет → `401`).
>
> **Доказательства, две плоскости.**
> 1. **Рантайм, прибор `tsx`.** Пустой файл → `36 признаков, все встроенные, из файла 0`. Файл с
>    четырьмя записями → принято **3 из 4**: запись без ключа выпала; чужой уровень `ZZZ` приведён
>    к `field`, чужой тип — к `text`; ключ `field.money; DROP TABLE tgdesk_messages` получил
>    `где живёт = имя недопустимо: таблицы нет`, то есть таблицы ему не будет.
> 2. **Собранный код на сервере.** `grep -rl fact_registry .next/server` → **0 файлов**.
>    Негативный контроль тем же прибором: `fact_` → **4 файла**, `intent.capture` → **2**, то есть
>    поиск работает, а таблицы определений в сборке действительно нет.
>
> 🛑 **HTTP-ПЛОСКОСТЬ ЗДЕСЬ НЕ РАЗЛИЧАЕТ, И ЭТО НАЗВАНО, А НЕ СКРЫТО.** Замок отвечает `307` и на
> `GET`, и на снятые `POST`/`PATCH` — положительный и отрицательный исход совпали. Доказательством
> взяты две плоскости выше.
>
> **Состояние:** `main` = `origin/main` = `/opt/fractera/telegrambot` = **`08d00d5`**,
> `TSC_RC=0`, `BUILD_RC=0`, порт 3600 держит потомок pm2 (`735816` ← `735815` ← pm2 `735801`).
> 🛑 Первый замер после `reload` дал `000` на всех адресах — **ошибка прибора**: снят через 4 с,
> служба ещё не привязалась к порту. Перемерян.
>
> **Что НЕ входило и осталось как было:** четыре файла есть на 3000 и отсутствуют здесь —
> `candidates.ts`, `collect.ts`, `lifecycle.ts`, `subjects.ts`. Конфиги службы (`APP-CONFIG`,
> `PLATFORM-CONFIG`, `DESIGN-CONFIG`) по-прежнему читаются из дерева слота 3000.

---

> # 🔒 ОТПРАВКА С СЕРВЕРА В GitHub ОТКРЫТА 2026-09-06 (137-16, доказано)
>
> **Владелец добавил Deploy key с правом записи**; отпечаток на GitHub
> `SHA256:7rhPFUSHRBuOip85w7xKUnK2RyV1LWlzQx1N+rEQxr4` совпал с `ssh-keygen -lf`
> нашего `/root/.ssh/telegrambot_deploy.pub` — ключ именно наш, а не чей-то ещё.
>
> 🔒 **АДРЕСА РАЗВЕДЕНЫ НАМЕРЕННО: чтение по HTTPS, отправка по SSH.**
> `fetch = https://github.com/Fractera/fractera-telegrambot-starter.git` ·
> `push = git@github-telegrambot:Fractera/fractera-telegrambot-starter.git`,
> алиас `github-telegrambot` описан в `/root/.ssh/config` с `IdentitiesOnly yes`.
> ✗ оплачено в тот же день: переключив remote ЦЕЛИКОМ на SSH до того, как ключ был добавлен,
> я сломал получение обновлений — сервер перестал видеть GitHub вовсе.
> **Отбирать работающее чтение ради будущей отправки нельзя.**
>
> 🔒 **ТОКЕН ВЛАДЕЛЬЦА НА СЕРВЕР НЕ ПОЕХАЛ, И ЭТО РЕШЕНИЕ, А НЕ ОБХОД.** Личный PAT даёт доступ ко
> ВСЕМ его репозиториям, а на этой машине живёт автономный агент канала. Deploy key ограничен одним
> репозиторием — измерено негативным контролем: `git ls-remote` в соседний репозиторий отвечает
> `ERROR: Repository not found`, `rc=128`.
>
> **Два доказательства из разных плоскостей, снятые 2026-09-06:**
> 1. **Аутентификация.** `ssh -T git@github-telegrambot` →
>    `Hi Fractera/fractera-telegrambot-starter! You've successfully authenticated`.
>    До добавления ключа тот же вызов давал `Permission denied (publickey)`.
> 2. **Настоящая ЗАПИСЬ, а не `--dry-run`.** Пробник `deploy-key-probe` создан на GitHub
>    (`* [new branch]`, виден в `ls-remote`) и удалён (`- [deleted]`, `ls-remote` пуст).
>    🔒 **`push --dry-run` права записи НЕ доказывает** — при `Everything up-to-date` он проходит
>    и на ключе только для чтения.
>
> Состояние на момент записи: `main` = `origin/main` = `04d58b3`, дерево чистое, лишних веток на
> GitHub нет (`main`, `new-step-97`, `new-step-101`).

---

# === 137-5 / 137-6: ПАДЕНИЕ СТРАНИЦЫ, ССЫЛКИ ПОДВАЛА, ДВА КЛЮЧА — 2026-09-06 ===

**Сервер:** `72c5dfe`, `BUILD_RC=0`, ⨯ в логе после рестарта — **0**.

## ✗ СТРАНИЦА ПАДАЛА, И СБОРКА ЭТОГО НЕ ЛОВИЛА

Владелец открыл `chat.aifa.dev/ru/settings` → «This page couldn't load». В логе —
`Error: useTheme must be used inside ThemeProvider`, трижды. **`TSC_RC=0` и `BUILD_RC=0` при
этом были зелёными**: отказ рантаймовый, лог сборки о нём молчит по устройству.

🔒 **ЛЕЧЕНИЕ — ОДИН ХОЗЯИН ТЕМЫ, А НЕ ВТОРОЙ ПРОВАЙДЕР РЯДОМ.** `providers/theme-provider.client.tsx`
стал переходником: наружу прежнее API, внутри `next-themes` чата. Обернуть сегмент вторым
провайдером было проще и было бы ловушкой: класс на `<html>` ставили бы двое, состояние жило бы в
двух местах, переключатели разошлись бы молча. Проверено: `createContext` во всём перенесённом коде
есть только у `DrawerProvider`, и он обёрнут.

## ✗ ССЫЛКИ ПОДВАЛА ВЕЛИ В 404 — НАШЁЛ ВЛАДЕЛЕЦ

Дословно: «эти ссылки наследуют твой префикс chat и поэтому теряют работоспособность». В источнике
адреса относительные, и **там это верно** — страницы свои. Приехав на 3600, те же адреса стали
указывать на `chat.<домен>`. 🔒 Префикс теперь из адреса сайта (`https://aifa.dev`, измерено в
`APP-CONFIG`); пустой адрес **прячет группу целиком** — ссылка в 404 хуже отсутствующей.

## 🔑 ДВА КЛЮЧА — ОДИН ВОПРОС ВЛАДЕЛЬЦА, ДВА РАЗНЫХ ОТВЕТА

**OpenAI — был дефект, исправлен.** Карточка писала «не задан» при живом ключе. Причина измерена:
в `.env.local` службы бота нет `DATA_SECRET`/`REMOTE_DATA_URL` — и не должно быть, секреты слоя
данных живут в файле СЛОТА. Перенесённый `data-service.ts` спрашивал только своё окружение.
Фолбэк на `slotEnv()` взят у рабочего кода этого же репозитория. **Измерено после правки:**
слой данных отвечает `200`, `{"app":{"configured":true},"data":{...},"graph":{...}}`, хвост `RSIA`;
негативный контроль с ложным секретом — `401`.
🛑 **Первый мой замер дал `401` и был ошибкой ПРИБОРА:** пробник слал `x-api-key`, а слой данных
ждёт `X-Data-Secret`. Заголовок надо брать из кода, а не из памяти.

**Anthropic — дефекта нет, ключа физически нет.** Проверено везде: `.env.local` слота и всех служб,
окружение процесса канала, `/root/.claude` — `ANTHROPIC_API_KEY` **отсутствует**. Что есть:
**вход по подписке** (`/root/.claude/.credentials.json`, `claudeAiOauth`, scope `user:inference`) —
именно на нём работает канал по закону шага 115, и API-ключ ему не нужен. Карточка говорит правду.
🔒 **«Третьего слоя» не будет:** `env-writer` получил фолбэк на `FRACTERA_SLOT_ENV`, поэтому ключ,
введённый на 3600, ляжет в файл СЛОТА — туда же, куда кладёт 3000. Без этой правки он лёг бы в
`.env.local` службы бота, и это был бы ровно третий адрес хранения.

## ✅ СТАРАЯ СТРАНИЦА НА 3000 ПЕРЕАДРЕСУЕТ — ЗАКРЫТО 2026-09-06 (137-8)

Слово владельца: «`https://aifa.dev/ru/architect/telegram` ??? why it not was removed?» — и он прав:
переадресация лежала в FNS (`b29506c`) и до слота не доехала. Правка слота сделана по этому его
требованию: закон разрешает её только прямым словом, и оно прозвучало дважды.

🛑 **У СЛОТА НЕТ REMOTE ВОВСЕ — ИЗМЕРЕНО, А НЕ ВСПОМНЕНО.** `git remote -v` в `/opt/fractera/app`
пуст, значит `git pull` невозможен и доставка идёт файлами.
🪦 **Запись федерального `CLAUDE.md` «у слота ЕСТЬ remote, `git pull` — нормальный путь обновления»
была верна на 2026-08-24 и устарела МОЛЧА.** Ровно поэтому там же стоит правило «проверять каждый
раз, а не помнить» — оно и отработало.

**Доставлено три файла:** страница-переадресация (296 строк → 52), `lib/site-urls.ts` (префикс
`chat` в `KNOWN_PREFIXES` — без него адрес собрался бы `chat.chat.<домен>`), подвал.
Сборка слота под `flock`, `BUILD_RC=0`, затем `pm2 reload`.

**Доказательства, две плоскости.** Живой ответ: `https://aifa.dev/ru/architect/telegram` → **308** →
`https://chat.aifa.dev/ru/settings`, одинаково локально и снаружи. **Негативный контроль:** соседняя
`/ru/architect/design` отвечает `200` — слой цел, упало не всё. Порт 3000 держит потомок pm2
(`723100` ← `sh -c next start` ← pm2 `723072`), `restarts 2`.

🛑 **В СЛОТЕ ОСТАЛИСЬ ЧУЖИЕ НЕЗАКОММИЧЕННЫЕ ПРАВКИ** — `APP-CONFIG/app-config.json`,
`package.json`, `package-lock.json`, `next-env.d.ts`. Я их не трогал: это состояние машины
владельца, а не моя работа.

🔒 **ЗАМКИ ПРОВЕРЕНЫ ИЗМЕРЕНИЕМ:** восемь дверей `api/architect/*` без куки — `307` (уводят на вход,
данных не отдают); страница на 3600 закрыта серверной `fracteraRoles()` → `architect` до рендера.

---

# === ШАГ 137: ЗЕРКАЛО СТРАНИЦЫ БОТА ПОЛНОЕ — 2026-09-06 ===

**Четыре раздела из четырёх, шапка и подвал те же, что на 3000, свой layout.**
Доставлено и собрано: `HEAD` сервера `6b58468`, `BUILD_RC=0`, порт 3600 держит потомок pm2.

```
fractera-telegrambot-starter  6b58468 = origin/main = /opt/fractera/telegrambot  порт 3600
fractera-easy-starter         6b6f0fb = origin/main  (две строки env в bootstrap.sh)
fractera-next-starter         b29506c = origin/main  (не трогали сегодня)
```

| Подшаг | Что сделано | Коммит |
|---|---|---|
| 137-2 | раскладка слоя перенесена дословно, мой самопал выброшен | `04725e8` |
| 137-3 | шапка, подвал, ящик + раздел «Настройки» — 115 файлов | `4464f5c` |
| 137-4 | «Логи» и «Паспорт» — заглушек не осталось | `6b58468` |

## 🔒 ДВА ТРЕБОВАНИЯ ВЛАДЕЛЬЦА, ЗАПИСАННЫЕ ДОСЛОВНО

> «add header and footer 1:1 as in port 3000 / Страницы должно быть выглядеть одинаково, ты
> подключаешь те же самые компоненты, но естественно layout ты создаёшь свой»

> «если по 3000 будет полностью удалён твой проект должен сохранить работоспособность, хедер и
> футер могут оставаться пустыми это нормально, твой порт из этого падать не должен»

🔒 **ОДИНАКОВЫЙ ВИД ОБЕСПЕЧЕН КОНСТРУКЦИЕЙ, А НЕ КОПИРОВАНИЕМ ЗНАЧЕНИЙ.** Оба конфига стартера
читают путь из окружения (`APP_CONFIG_PATH`, `PLATFORM_CONFIG_PATH`) — **способность существовала,
я её не изобретал.** Две строки добавлены в `bootstrap.sh` (новые серверы) и в `.env.local` текущего.

🔒 **ЖИВУЧЕСТЬ БЕЗ 3000 ИЗМЕРЕНА ДВАЖДЫ — ЛОКАЛЬНО И НА СЕРВЕРЕ.** Служба поднята на 3699 с путями
к несуществующим конфигам: `/ru/settings` → `307`, **ошибок в логе 0**. Механизм: оба конфига ловят
отсутствие файла (`try/catch` → умолчания), `getMenuGroups` сканирует СВОЮ файловую систему и тоже
под `try/catch`. Меню будет пустым — служба стоит.

## 🛑 ЧЕТЫРЕ АДАПТАЦИИ — ВСЁ, ЧЕМ ЗЕРКАЛО ОТЛИЧАЕТСЯ ОТ ИСТОЧНИКА

Каждая названа в самом файле, чтобы `diff` не выглядел случайным.

1. `components/ui/dialog.tsx` (вендор ai-chatbot) получил необязательный `closeLabel` — аддитивно.
2. Год копирайта в подвале зовёт `new Date()`; под `cacheComponents` это отказ сборки. Лечение —
   адаптер `connection()` в СВОЁМ layout, а не правка перенесённого файла.
3. Три островка направлены на существующие двери зеркала (`api/fractera/{openai-key,agent-setup,
   transcribe}`). ✗ вторая дверь к одному хранилищу — ошибка, оплаченная 109-3.
4. В восьми перенесённых дверях сняты `runtime`/`dynamic` — 16 ошибок сборки.

🔒 **ЛИНТЕРНОЕ ИСКЛЮЧЕНИЕ ЗЕРКАЛЬНЫХ ФАЙЛОВ СТАЛО ПАПОЧНЫМ.** Поимённый список на 124 файла никто
не поддержит, и первый забытый файл сделал бы вердикт линтера случайным. Четыре файла всё же
поимённо (`lib/fractera`, `lib/auth`): папки общие, и исключение целиком сняло бы линтер с кода,
написанного здесь. 🛑 Цена записана в конфиге: дефект, приехавший из источника, здесь не покраснеет.

## ▶ ЧЕГО НЕТ, И ЭТО НАЗВАНО

- 🛑 **Крошек и JSON-LD в шапке нет:** их даёт `PageHeader`, а он тянет `Breadcrumbs` → `lib/jsonld`.
  Разметка заголовка взята из него дословно.
- 🛑 **Паспорт читается из СВОЕЙ папки** (`/opt/fractera/telegrambot/development-docs/PASSPORT.md`),
  файла пока нет — раздел говорит об этом словами. **Чей паспорт должен стоять на этом экране —
  вопрос владельца:** читать папку слота значило бы связать две службы там, где связи не объявляли.
- **Долг учёта:** плана шага 137 в `new-steps/` нет, итогов 136-8, 137-1…137-4 нет.
- **Не показано владельцу.** Своими приборами содержимое не снять: замок отдаёт `307` и на настоящий
  адрес, и на заведомо ложный `/xx/settings` — негативный контроль совпал с положительным.

---

# === ШАГ 137 В РАБОТЕ 2026-09-06 — ПЕРЕНОС СТРАНИЦЫ БОТА С 3000 НА 3600 ===

**Задача владельца:** страница Telegram-бота уезжает из слоя архитектора (`:3000`) в службу бота
(`:3600`, `chat.<домен>/{lang}/settings`). Дословно: «если пользователь, которому не нужен сайт,
придёт только на чат-бот, то ему будет достаточно всей информации».

```
fractera-telegrambot-starter  04725e8  = origin/main  = /opt/fractera/telegrambot  порт 3600
fractera-next-starter         b29506c  = origin/main  (старый адрес → переадресация сюда)
```

## 🔒 ГЛАВНОЕ РЕШЕНИЕ ДНЯ — СЛОВО ВЛАДЕЛЬЦА, ЗАПИСАНО ДОСЛОВНО

> «вместо того чтобы перенести один к одному тот, который существует, ты попытался создать свой
> собственный дизайн… превратил это в просто жуткий кошмар… немедленно прекратить создавать новые.
> Твоя задача — перенести существующую архитектуру, а не создавать это уродство»

✗ **ЧЕМ ОПЛАЧЕНО.** Подшаг 137-1 (вчера, `c35fd8d`) перенёс **2 файла из 15**, а остальное собрал
СВОЕЙ вёрсткой: `max-w-4xl` вместо ленты страницы, ряд круглых ссылок вместо меню слоя, самодельная
жёлтая плашка вместо общей заглушки. **Закон стоял в корпусе до этого шага** — «при переносе экрана
копируется поведение и текст» (77-9), «максимальное перемещение с минимальной адаптацией», — и я
его не открыл. Признак, который обязан был меня остановить: **вторая реализация того, что уже
работает, — решение владельца, а не моё.**

## ✅ 137-2 ЗАКРЫТ И ДОСТАВЛЕН — `04725e8`

Перенесено **дословно, побайтно равно источнику** (проверено `diff`): `workspace-shell.tsx` (282,
меню + правая колонка + ящик на телефоне) · `section-intro.client.tsx` (89) · `in-progress.tsx` (43)
· `auto-refresh.client.tsx` (51) · `architect-layer.i18n.ts` (91) · `passport-outline.ts` (48).
В `globals.css` приехали `--app-w`, `--page-py-work`, `[data-app-column]` теми же значениями.

🔒 **ЗЕРКАЛЬНЫЕ ФАЙЛЫ ИСКЛЮЧЕНЫ ИЗ ЛИНТЕРА, А НЕ ПОПРАВЛЕНЫ ПОД НЕГО.** Здешний линтер строже
стартерного — 10 замечаний в чужих файлах. Правка сломала бы `diff`, единственный прибор, которым
видно, что зеркало не разошлось; чинить надо в источнике, одной правкой на обе копии. Тот же приём,
которым в `biome.jsonc` уже исключён вендоренный `components/ai-elements`. 🛑 **Цена названа в самом
конфиге:** настоящий дефект, приехавший из источника, здесь тоже не покраснеет.

🛑 **ГЛОБ В JSON: `[lang]` ЗАПИСЫВАЕТСЯ КАК `**`.** Квадратная скобка в glob — класс символов, а её
экранирование `\[` недопустимо в JSON вовсе. Три попытки дали отказ конфигурации, то есть линтер не
запускался совсем — и выглядело это как «проверка прошла».

**Два дефекта 137-1 закрыты той же правкой:** плашка звала вернуться «туда, где было раньше», а
старый адрес тем же днём стал переадресацией СЮДА (человек ходил бы по кольцу); и она была
хардкодом только по-русски на двуязычной странице.

**Доказательства, две плоскости:** `diff` шести файлов с источником — идентичны, негативный
контроль (`page.tsx`) отличается · разметка на СЕРВЕРЕ: `data-workspace` 16, самопал
`rounded-full bg-primary px-3 py-1.5` — 0, вчерашняя плашка — 0, ложная строка — 0.
**Доставка:** `HEAD` сервера `04725e8`, `BUILD_RC=0`, порт 3600 держит `node server.mjs` pid 717208
← `sh -c` 717207 ← pm2 717193, то есть потомок pm2, а не сирота.

## ▶ СЛЕДУЮЩЕЕ ДЕЙСТВИЕ

**Показать владельцу экран и получить его слово** — закон согласования построенной поверхности.
Адрес: `https://chat.aifa.dev/ru/settings`. 🛑 Своими приборами содержимое не снять: замок отдаёт
`307` и на `/ru/settings`, и на заведомо ложный `/xx/settings` — негативный контроль совпал с
положительным, значит замер не различает.

**Дальше в очереди, по одному подшагу:**
- **137-3** — «Настройки»: `telegram-settings.tsx` (171) + `telegram-schedule.client.tsx` (91) +
  `lib/architect/channels.ts` (174), ходит в службу каналов `:3500`;
- **137-4** — «Логи»: `task-parse-section.tsx` (283) + диалоги;
- **137-5** — «Паспорт»: `passport-body.client.tsx` + `ai-elements/message`;
- **137-6** — снос мёртвого куста в слое 3000 и починка ложного надгробия в его `page.tsx`
  (комментарий утверждает, что компоненты удалены, — они на месте, у них ноль потребителей);
- **долг учёта:** плана шага 137 в `new-steps/` нет вовсе, итогов 136-8 и 137-1 нет.

🛑 **ШАПКА ПЕРЕНЕСЕНА БЕЗ КРОШЕК, И ЭТО НАЗВАНО, А НЕ ЗАБЫТО.** На 3000 её рисует `PageHeader`, он
тянет `Breadcrumbs` → `APP-CONFIG`, `PLATFORM-CONFIG`, `lib/jsonld` — хранилища слота, которых на
3600 нет. Разметка заголовка взята из него дословно; крошки и её JSON-LD не приехали.

---

# === ПЕРЕДАЧА СЕССИИ 2026-09-05 23:40 — ЧИТАТЬ ПЕРВЫМ ===

**Шаг 136 — починка первого живого развёртывания.** Владелец поднял чистый сервер и получил
молчащего бота после часа ожидания. Найдено и закрыто пять причин, все разные.

```
fractera-telegrambot-starter  6c752de  ↔  /opt/fractera/telegrambot     порт 3600
fractera-easy-starter         12ac967      lib/bootstrap.sh
```

## ▶ СЛЕДУЮЩЕЕ ДЕЙСТВИЕ

**Владелец делает новое развёртывание с нуля.** Это первая полная проверка пути на чистой
машине — прежняя прошла до того, как были найдены `bun`, две сессии канала и `markdownv2`.

Смотреть в журнале установки строки `[telegrambot]`. Обязаны быть:

```
+ bun <версия>          + telegram установлен       + hasTrustDialogAccepted
+ claude-plugins-official                            опрашиватель Telegram: 1 — верно
```

🛑 **Строка «опрашиватель Telegram: 0» означает молчащего бота** — смотреть `bun`, токен, экран
сессии (`screen -r fractera-agent`). Строка `«$N — их больше одного»` означает две сессии канала.

## 🔒 ПЯТЬ ПРИЧИН, НАЙДЕННЫХ ЭТИМ ВЕЧЕРОМ — не переоткрывать

| # | Причина | Чем закрыта |
|---|---|---|
| 1 | **`bun` не ставил никто.** Опрашиватель плагина — `bun server.ts`; без него канал мёртв | `bootstrap.sh` шаг `chat_bun` + `install.sh`, оба со ссылкой в `/usr/local/bin` |
| 2 | **Две сессии канала.** `install.sh` поднимал канал под pm2, а кнопка вкладки заводила вторую | `server.mjs`: режим `claude-channel` = `screen -r fractera-agent` |
| 3 | **`markdownv2` глотал ответы целиком.** Точка в адресе — служебный символ | правило в `CLAUDE.md`: `format` не передавать |
| 4 | **Модальный вопрос о доверии папке** держал агента под pm2 | `hasTrustDialogAccepted` в `install.sh` |
| 5 | **Плагин Telegram не ставил никто** | `claude plugin install … -y` в `install.sh` |

🔒 **ОБЩЕЕ У ВСЕХ ПЯТИ: ОТКАЗ ВЫГЛЯДЕЛ КАК УСПЕХ.** `pm2 online`, вход выполнен, токен на месте,
порт отвечает — и бот молчит. Единственный замер, отличающий живой канал от мёртвого:
`pgrep -fa "bun run --cwd" | wc -l` = 1. Он теперь печатается установщиком.

🔒 **ЗАКОН, ОПЛАЧЕННЫЙ ДОРОЖЕ ВСЕХ: ИСТОРИЯ БЫЛА НА МЕСТЕ, Я ЕЁ НЕ ПРОЧИТАЛ.** Правило «кнопка
в терминале подключает, а не запускает» стояло в `/code/CLAUDE.md` и комментарием в
`terminal-panel.client.tsx` **до** шага 135. Я добавил автозапуск канала под pm2, не сверившись
с ним, и потом три часа искал причину, которую сам же и создал.
**Перед правкой канала — читать существующие законы о канале.**

🔒 **ИНСТРУКЦИЯ АГЕНТА — РАБОЧАЯ КАРТОЧКА, А НЕ РАЗБОР.** Слово владельца: «эта инструкция может
поместиться на 20 строчках… а ты превратил это в шекспира». Цена измерена журналом сессии:
32 599 байт перечитывались на каждом сообщении — **97,9 с размышления и 34,6 с генерации** на
ответ «я тут», при том что оба инструмента отработали меньше двух секунд.
Сжато до 5 324 байт / 75 строк. Разборы «чем оплачено» живут в git (`00e530c`) и в
`completed-steps/`, а не в файле, который модель читает каждый раз.

🔒 **КЛАСС `position` У ЧУЖОГО КОМПОНЕНТА.** `relative`, дописанный `DialogContent`, молча отменил
его `fixed` — окно уехало на пол-экрана вниз. Проверять, чем компонент уже позиционирован, ДО
правки.

## ✅ Что закрыто из долгов шага 133 — все три

1. `fakechat` выключен и больше не ставится (`install.sh` снимает его, если найдёт).
2. Точные имена инструментов в `CLAUDE.md` + запрет вызывать MCP через Bash.
3. Текст заявки формулировкой владельца в `runRequest()`.

## 🛑 Что известно как непроверенное

- **Скорость после сжатия инструкции не измерена.** Сжали в 6 раз, но замера «было/стало» на живом
  сообщении ещё нет. Первый же ответ бота его даст.
- **Модель `haiku`** стоит умолчанием в `claude-channel.sh`. На длинном контексте думает долго;
  `sonnet` быстрее и дороже. Решение владельца не запрашивалось.
- **Что остаётся за человеком по природе:** вход в Claude по подписке и токен бота с allowlist.
  Установщик их проверяет и называет недостающее строкой в журнал.

## 🗒 Открытый долг среды

`corepack`, идущий в комплекте с Node 22.11, не проверяет подписи npm и ломает `pre-commit`
(`lint-staged` → `corepack pnpm run fix`). Обход на время сессии — `COREPACK_INTEGRITY_KEYS=0`.
Настоящее лечение — обновить Node или снять индирекцию `corepack` из конфига репозитория.
Решение владельца не запрашивалось.

---

# === ПРЕДЫДУЩАЯ ПЕРЕДАЧА 2026-09-05 21:10 (история) ===

**Где мы:** шаги 134 и 135 закрыты и доказаны живьём. Агент переехал жить в репозиторий своей
автоматизации; на боевой машине не осталось ничего, положенного руками.

```
fractera-telegrambot-starter  ↔  /opt/fractera/telegrambot     порт 3600
pm2: fractera-telegrambot · fractera-agent-channel · fractera-limit-watch
```

**Доказано владельцем в Telegram 20:22 и 20:47:** «Сидю в `/opt/fractera/telegrambot`» ·
«Живу на домене `aifa.dev`, `https://chat.aifa.dev/terminal`». Негативный контроль — тот же вопрос
в 19:55 давал `/opt/fractera/agent-workspace` и голый IP.

Итоги: `completed-steps/134-main.md`, `completed-steps/135-main.md`.

---

## ▶ СЛЕДУЮЩЕЕ ДЕЙСТВИЕ — две починки из трёх, названных владельцем 2026-09-05

Первая (выключить `fakechat`) **сделана** шагом 135: плагин снят из `enabledPlugins`, установщик его
больше не ставит и снимает, если найдёт. Проверено: `claude plugin list` → `fakechat … disabled`.

### 1. Дать в инструкции агента ТОЧНЫЕ имена и порядок вызовов

Сейчас `CLAUDE.md` говорит «вызови инструмент `intake`». Модель `haiku` не выводит из этого полного
имени и ищет. Вписать дословно:

```
mcp__intake__intake
mcp__intake__request_development
```

и порядок для файла: `download_attachment` → `intake(path)`. Плюс **прямой запрет вызывать
инструменты через Bash**.

✗ **Чем оплачено:** первый поиск инструментов вернул пустоту (из-за `fakechat`), и агент **девять раз
имитировал вызовы в Bash** — `echo "Calling MCP tool not available via bash"`, — каждый раз спрашивая
у владельца разрешение.

🛑 Проверено 2026-09-05 21:10: `grep -c mcp__intake__intake CLAUDE.md` → **0**. Не сделано.

### 2. Переписать текст заявки формулировкой владельца

Дословно, брать без правок:

> Запустить разработку внутри этого чата вы не можете. Ваше задание перемещено в специальную папку,
> где хранятся предварительные заказы на разработку. Чтобы попросить агента-программиста принять этот
> документ в работу, перейдите в Telegram-бот агента разработки Fractera.

**Где править:** `runRequest()` в `scripts/agent/intake-preloader.js`.

🛑 Проверено 2026-09-05 21:10: строки в файле нет. Не сделано.

---

## 🔒 Законы, добытые шагами 134–135 — не переоткрывать

- **Каждая новая способность агента под `pm2` приносит свой модальный вопрос.** Четвёртый и пятый
  случаи за сутки: свой `.claude/settings.json` вызвал вопрос о доверии папке; `claude plugin install`
  спрашивает подтверждение без `-y`. Оба раза `pm2` писал `online`, а канал молчал.
- **Конфигурация восстанавливается измерением рабочей машины, а не пересказом в журнале шага.** Имена
  разрешений в записи 133 не совпали с настоящими (`mcp__plugin_telegram_telegram__reply`).
- **Устаревшее значение опаснее отсутствующего.** `NEXT_PUBLIC_AUTH_URL` в `.env.local` несёт IP,
  записанный при рождении сервера; файл читается, выглядит свежим и врёт.
- **«Не импортируется» и «не вызывается» — разные утверждения.** `lib/db/migrate.ts` недостижим по
  графу импортов и жив: его зовёт `package.json`.
- **Установщик живёт рядом с тем, что ставит.**
- **`CLAUDE.md` в корне читают двое, и разделить их нельзя** — файл адресуется обоим явно.

## 🛑 Что известно как непроверенное

**Весь путь на по-настоящему чистой машине не прогонялся ни разу.** `install.sh` работал только там,
где `node_modules`, `claude` и плагин уже стояли. Ближайшее развёртывание владельца — первое
настоящее доказательство.

**Что останется за человеком в любом случае:** вход в Claude по подписке и токен бота с allowlist.
Установщик их проверяет и называет недостающее строкой в журнал.

---

# === ПРЕДЫДУЩАЯ ПЕРЕДАЧА 2026-09-05 19:05 (история) ===

**Где мы:** шаг 133 «Первая волна приёма» построен и работает. Открыты три починки, найденные
живым испытанием владельца в 18:53–18:56. Ничего не сломано; всё перечисленное — улучшения.

---

## ▶ СЛЕДУЮЩЕЕ ДЕЙСТВИЕ — три починки, все три названы владельцем

### 1. Выключить плагин `fakechat` — он ломает поиск инструментов

```
"No matching deferred tools found. Note: these configured MCP servers failed to connect:
 plugin:fakechat:fakechat — Skipping connection (recent failure cached, retries in 15 min)"
```

🔒 **МЕХАНИЗМ.** Инструменты у агента **отложенные** (deferred): он не видит их сразу и ищет через
`ToolSearch`. Первый поиск вернул пустоту из-за отказа `fakechat` — и агент **девять раз имитировал
вызовы в Bash** (`echo "Calling MCP tool not available via bash"`, `Placeholder`), каждый раз спрашивая
у владельца разрешение. Со второго поиска, точным списком имён, инструменты нашлись.

**Что сделать:** в `/root/.claude/settings.json` убрать `fakechat@claude-plugins-official` из
`enabledPlugins`. Бэкап уже есть: `/root/revision-117-backup/settings.json.before-mcp`.

### 2. Дать в инструкции агента ТОЧНЫЕ имена и порядок

Сейчас `CLAUDE.md` агента говорит «вызови инструмент `intake`». Модель `haiku` из этого не выводит
полного имени и ищет. Нужно вписать дословно:

```
mcp__intake__intake
mcp__intake__request_development
mcp__plugin_telegram_telegram__download_attachment
```

И порядок для файла, которого сейчас нет:
**1)** `download_attachment(file_id)` → путь · **2)** `intake(path=…, chat_id=…, kind=voice|photo)`.
🛑 **И ПРЯМОЙ ЗАПРЕТ:** не пытаться вызывать инструменты через Bash. Агент потратил на это две минуты
и девять запросов разрешения.

### 3. Переписать текст ответа о заявке — формулировка владельца

✗ **Мой текст был непонятен, и он прав.** «Запустить в разработку можно через Telegram-бота агента
разработки» не говорит главного: **в этом чате разработку запустить нельзя.**

**Его формулировка, брать дословно:**

> Запустить разработку внутри этого чата вы не можете. Ваше задание перемещено в специальную папку,
> где хранятся предварительные заказы на разработку. Чтобы попросить агента-программиста принять
> этот документ в работу, перейдите в Telegram-бот агента разработки Fractera.

**Где править:** `runRequest()` в `scripts/agent/intake-preloader.js` (и на сервере
`/opt/fractera/intake-preloader.js`).

---

## ✅ ЧТО РАБОТАЕТ — доказано живьём, не трогать

```
Telegram → агент → mcp__intake__intake → медиатека + вектор + граф → текст агенту
                 └→ mcp__intake__request_development → заявка → номер человеку
```

**Полный цикл прошёл на настоящем голосовом владельца 18:55:54** — расшифровано, разложено,
заявка создана (`05-09-2026_18-56-15.md`), ответ доставлен в Telegram.

| Что | Где живёт |
|---|---|
| дверь приёма (без ответа) | `fractera-next-starter/app/api/intake/route.ts` |
| дверь заявки | `.../app/api/intake/request/route.ts` |
| два источника байтов | `processBytes` в `branches/files.ts`, `Incoming.fileBytes` |
| пятый повод приёмной | `fromAgent` в `lib/architect/pre-step-writer.ts` |
| MCP-сервер | `/opt/fractera/intake-preloader.js` (копия в `scripts/agent/`) |
| объявление сервера | `/opt/fractera/agent-workspace/.mcp.json` |
| инструкция агента | `/opt/fractera/agent-workspace/CLAUDE.md` (копия в `scripts/agent/AGENT-CLAUDE.md`) |

**Настройки агента** (`/root/.claude/settings.json`):
`enabledMcpjsonServers: ["intake"]` · `permissions.allow`: `reply`, `download_attachment`,
`mcp__intake__intake`, `mcp__intake__request_development`.

## 🔒 Законы, оплаченные этими сутками — не переоткрывать

- **Каждая новая способность агента под `pm2` приносит свой модальный вопрос**, и закрывать его надо
  настройкой в той же правке. Три случая за сутки: `--add-dir`, `reply`, MCP-сервер.
- **Инструкция, добавленная к чужой, конкурирует с ней, а не дополняет.** Своя ближе — значит
  перетягивает поведение, и всё, о чём умолчала, исполняется хуже. Оплачено: агент перестал звать
  `reply` и писал в ленту.
- **Перевод строки собирается `String.fromCharCode(10)`**, не escape-последовательностью. Четыре
  случая за день.
- **Гейты прогоняются ДО доставки** (`check:api` поймал имя маршрута в 13 слов после отправки).
- **`node --check` проверяет синтаксис, а не связность.** Для скрипта, правленного программно,
  честная проверка — запуск.
- **У графа знаний `success` означает «принято», а не «сработало»** — проверять состоянием документа.

## 🛑 Открытые долги — не потерять

- **Вторая волна переописания** — не построена намеренно: нет правила запуска. Открытый вопрос
  владельцу: реестр решает, что кейс есть (**рекомендую**), или агент по смыслу разговора.
- **Финансовый учёт — только флаг.** `field.money` это `flag`, ответ строится как
  `COUNT(*) WHERE has_financial = 1`. Нужны `amount`(число) · `currency` · **`direction`
  (потратил/заработал)** · `counterparty` · `items` · `doc_id`. Принадлежат второй волне.
- **Род `general`** — вопрос о мире, который никуда не пишется. `question` описан как «о своей
  жизни» и пример владельца не покрывает.
- **Таблица внешних ссылок** — единственное, что заводится с нуля.
- **Крючки** календаря и карты, **теги пятью кандидатами**.
- **Третий маршрут в архитектуре:** было «относится к агенту / не относится», стало три — запись и
  чтение · **разработка (заявка)** · вопрос. Требует обновления `ARCHITECTURE.md`.
- **Слой архитектора отдаёт страницу без сессии** (`/ru/architect/telegram` → 200). Секреты не
  утекают, структура — да. В `/code/development-docs/BACKLOG.md`.
- **HTTPS по IP:80 без переадресации** — ограничение, не дефект: сертификат на IP не выдаётся.

## Состояние машины

```
pm2: 11 процессов · claude 1 · опрашиватель 1 · предзагрузчик 1 · очередь Telegram 0
модель агента: haiku (умолчание в /opt/fractera/claude-channel.sh, переживает удаление файла)
режим: --permission-mode auto (ставит пусковой файл, покрывает ВСЕ старты)
сессия: screen fractera-agent · подключиться: screen -r fractera-agent (выйти Ctrl+A, D)
защищённый режим: FRACTERA_IP_NODOMAIN_MODE=false · ufw active (22/80/443) · TLS есть
```

**Репозитории:** `fractera-next-starter` d584ca6 · `fractera-chatbot-starter` 223eed6 ·
`/code` 8295ef7 (не пушится по закону). Все — `main == origin/main`.

---

# === current-steps.md — читается ПЕРВЫМ в каждой сессии, до плана шага и до кода ===

**Репозиторий:** `fractera-chatbot-starter` → `github.com/Fractera/fractera-ai-chat-starter`.
**Здесь живут И код, И учёт** — решением владельца 2026-09-03, дословно: «код и учёт переезжают в
fractera-chatbot-starter».

🪦 **ВТОРАЯ ПОЛОВИНА ЭТОЙ ШАПКИ ОТМЕНЕНА 2026-09-05 (ревизия, шаг 116).** Здесь стояло: «Хозяином
правды о разговоре стал Postgres этого репозитория; Telegram — вход и выход, а не место, где разговор
происходит». Действующее устройство — прямо противоположное, и оно ниже.

---

# 🔒 РЕВИЗИЯ 2026-09-05 — ЧИТАТЬ ДО ВСЕГО ОСТАЛЬНОГО В ЭТОМ ФАЙЛЕ

**Решение владельца 2026-09-05, дословно:** «мы полностью прекращаем стратегию одновременного
использования библиотеки чата и Telegram, так как подтвердилась гипотеза простого и эффективного
взаимодействия напрямую между Telegram и Claude Code».

## Что теперь единственный путь к ИИ

```
человек в Telegram → плагин каналов Anthropic → живая сессия claude на сервере → обратно в Telegram
```

Нашего кода в этой цепочке ноль. Доказано шагом 115 и подтверждено владельцем дословно: «all work».
Устройство, адреса и три запертых решения — `completed-steps/115-main.md`; сторож лимита подписки —
`completed-steps/115-3.md`.

## 🔒 ДВА БОТА, И ИХ ВСЕГДА РОВНО ДВА

Прямое решение владельца 2026-09-05: **вместо множества ботов в проекте всегда два, у каждого своя
работа.** Третьего не заводить — ни «для теста», ни «временно».

| Бот | Чем занят | Состояние |
|---|---|---|
| **№1 — управление агентами и автоматизация** | основная задача проекта; это тот бот, что живёт под pm2 (`fractera-agent-channel`) | ✅ работает с 2026-09-05 |
| **№2 — управление разработкой** | ещё не начат и не описан | 🛑 **не является центром архитектуры** — прямое слово владельца |

🔒 **ВТОРОЙ БОТ НЕ ПРОЕКТИРУЕТСЯ ВПЕРЁД.** Владелец назвал его существование и назвал его местом
на периферии; всё остальное о нём — домыслы. Действует общий закон «дальнее не проектируется вперёд»
(федеральный `CLAUDE.md`, решение 2026-09-02).

## Что этой ревизией отменено

| 🪦 Отменено | Чем заменено |
|---|---|
| одновременная работа библиотеки чата и Telegram | один путь: Telegram → Claude Code |
| «Telegram — не главный вход» (закон ниже) | Telegram — **единственный** вход к ИИ |
| «нативное хранилище чата — единственный источник данных» (закон шага 97) | живой корпус переписки у Anthropic и Telegram; см. цену ниже |
| «Стратегия автоматизации»: ручной конвейер OpenAI ↔ Anthropic Agent SDK | выбора нет; раздел и его код удалены |
| ссылка на чат в подвале сайта | удалена целиком вместе с расчётом адреса |
| «Вариант A — один бот, много собеседников» как открытый вопрос | ровно два бота, роли названы |

## 🛑 Цена, названная, а не замолчанная

- **Живой корпус переписки теперь не у нас** — он у Anthropic и в Telegram. Закон шага 97
  о единственном хранилище новой цепочкой **не исполняется**, и это выбор, а не поломка.
- **Каналы — research preview.** Флаги могут измениться; в тот день продукт останется без ИИ.
- **Квота одна на двоих.** Серверный канал работает по подписке владельца: его локальный Claude Code
  и бот черпают из одного пятичасового окна. Сторож лимита (115-3) об этом предупреждает, но не
  отменяет.

## Что остаётся живым и НЕ трогается этой ревизией

Служба каналов `:3500` (отправка, файлы, журнал), чат `:3600` как служба, медиатека, граф знаний,
ключ OpenAI (голосовой ввод и граф живут на нём). Ревизия убрала **стратегию и её поверхности**,
а не службы.

## 🛑 Одно решение владельца, которое ревизия НЕ приняла за него

**Судьба разбора запроса и реестра признаков** (шаги 81–95, 106; ~36 встроенных признаков, шесть
ступеней конвейера) — самый большой построенный кусок. Claude Code думает сам, и конвейер этой
цепочке не нужен. Но это отдельное решение о сносе работающего, а не следствие ревизии: шаг 106
переведён в **приостановлен**, а не отменён. Спросить владельца, когда он вернётся к теме.

---

## ✅ ГРУППА ЗАКРЫТА 2026-09-03 — TELEGRAM ↔ ВЕБ-ЧАТ, ПЕРВАЯ РАБОЧАЯ ВЕРСИЯ

Шаги 97, 98, 99, 100, 101, 103, 104, 105 закрыты; 102 построен и в тот же день отклонён владельцем
(`git revert`). Репорт фичи — что умеет, чем доказано, что осталось долгом, решения владельца
дословно, где живёт код:
`development-docs/reports/feature-telegram-web-chat-round-trip-with-multi-bot-mirroring-and-attachments.md`.
Итоги отдельных шагов — `completed-steps/97-main.md` … `105-main.md`.

Этот файл обнулён до этой точки той же операцией (закон §6 навыка `use-development-steps`): ниже —
знание о среде и законы репозитория, которые не стирает обнуление, а не состояние текущей работы.

---

## ✅ РАЗВЁРТЫВАНИЕ 2026-09-04 — ЧИСТОЕ, ВСЁ СЕГОДНЯШНЕЕ НА МЕСТЕ

```
8 процессов pm2, у ВСЕХ restarts=0, uptime растёт, сирот нет
8 портов слушаются · сайт 200 · панель 200 · чат 200
платформа 863a43e · чат 88d2a7c · слот — 1 коммит, содержимое сверено ПО ФАЙЛАМ
```

🔒 **ХЭШ СЛОТА НЕ СОВПАДАЕТ С FNS И НЕ ДОЛЖЕН:** при рождении история отвязывается, коммит
пересоздаётся. Сверять надо содержимое — сделано: 109-2 в слоте есть, 109-1 в службе данных есть.

**Ключ OpenAI доехал до всех трёх потребителей** (владелец ввёл новый, хвост `RSIA`):
дверь `:3300` и плашка слоя архитектора `:3000` говорят одно и то же —
`app=true · data=true · graph=true`. У чата своя переменная пуста, и это верно:
он читает файл слота, как и слой данных.

✅ **ТРЕТЬЕ ДОКАЗАТЕЛЬСТВО 109, КОТОРОЕ Я ОБЪЯВЛЯЛ НЕДОСТИЖИМЫМ, ПОЛУЧЕНО:** в режиме по IP
(`FRACTERA_IP_NODOMAIN_MODE=true`, обход авторизации) дверь слоя архитектора отвечает без сессии.
🔒 **Приём на будущее: недостижимое доказательство стоит перепроверить при СМЕНЕ РЕЖИМА СЕРВЕРА** —
недостижимость была свойством режима, а не задачи.

🛑 **Граф пуст (0 документов) — это ожидаемо:** сервер рождён заново, ничего не загружали.

## ✗ НО БОТ ОТВЕЧАЛ НЕ ЧЕРЕЗ ЧАТ — НАЙДЕНО И ПОЧИНЕНО 2026-09-04

Владелец написал боту после развёртывания и получил «В базе знаний пока ничего нет». **Это старая
логика ответа через граф знаний, а не чат.** В базе чата было 0 сообщений и 0 разговоров.

```
config.json службы каналов:  hookUrl ПУСТ · hookSecret ПУСТ
окружение слота:             CHANNELS_HOOK_SECRET отсутствовала
слот несёт /api/telegram/hook — старый путь ответа через граф
```

🔒 **МЕХАНИЗМ:** служба каналов не знала адреса чата и отдавала входящее старому хуку в слоте.
Граф на новом сервере пуст — отсюда тот самый текст. **Ни одной ошибки в логах: отказ молчаливый.**

✗ **ПРИЧИНА — СВЯЗКА БЫЛА НАСТРОЕНА РУКАМИ НА ПРЕЖНЕЙ МАШИНЕ (шаг 97) И НЕ ПОПАЛА В `bootstrap.sh`.**
Тот же закон, которым оплачено отсутствие самого чата в bootstrap: сделанное руками живёт на одной
машине.

✗ **И ЧЕМ ОПЛАЧЕНА ГАРАНТИЯ АГЕНТА.** Перед развёртыванием он сверил репозитории, код и сборки — и
**не проверил конфигурацию рантайма, которой в git нет**. 🔒 **«Всё отправлено» и «всё будет работать»
— разные утверждения**, и второе требует проверить то, что живёт ТОЛЬКО на машине.

✗ **ВТОРАЯ ОШИБКА, ПРЯМО В ПОЧИНКЕ, И ОНА ПОУЧИТЕЛЬНЕЕ ПЕРВОЙ.** Агент записал `CHANNEL_SECRET` в файл
чата, тогда как чат ждёт **`CHANNELS_HOOK_SECRET` из окружения СЛОТА**
(`lib/fractera/channels.ts:61`). **То есть он совершил ровно тот дефект, который весь день чинил
шагом 109: имя переменной принадлежит потребителю.** 🔒 Знание закона его не исполняет — второй раз
за сутки подтверждено измерением.

**Починено на сервере:** `hookUrl = http://127.0.0.1:3600/api/channels/inbound`, общий секрет в двух
местах. Копия прежнего конфига — `config.json.before-hookfix`.
**Доказано:** без секрета `401` · с секретом `400 unknown channel` (замок пройден) · настоящее
сообщение → `200`, создан разговор и сообщение в базе чата.

✅ **ШАГ 110 ЗАКРЫТ 2026-09-04, И ВЛАДЕЛЕЦ ПОДТВЕРДИЛ ЖИВЬЁМ: «now it work».**

**Причин было ТРИ, и каждая маскировала следующую:** пустой адрес доставки → уезжало в старый хук
слота · боты живут в `telegramBots[]`, и адрес — свойство БОТА, а не одиночной записи `telegram.*` ·
**служба и чат ждут секрет под РАЗНЫМИ именами** (`TELEGRAM_HOOK_SECRET` против
`CHANNELS_HOOK_SECRET`), а адрес служба брала из третьего места.

🔒 **КТО ЧТО ЧИТАЕТ — ИЗМЕРЕНО ПО ИСХОДНИКАМ, НЕ УГАДАНО.** Все три живут в окружении СЛОТА:
`CHANNELS_HOOK_SECRET` → чат · `TELEGRAM_HOOK_SECRET` → служба каналов · `CHANNELS_HOOK_URL` →
она же. Два имени у одного секрета — действующий контракт двух служб, а не опечатка.

**Починено в источнике:** FES `fa0faae` — bootstrap пишет три строки в окружение слота, у каждой
назван свой читатель; `ai-workspace` `5aafdfe` — `appHookUrl()` читает адрес оттуда же, откуда
`appSecret()` читает секрет. Итог: `completed-steps/110-main.md`.

🔒 **ГЛАВНЫЙ УРОК ДНЯ, ГОДНЫЙ ШИРЕ ШАГА: У ДОКАЗАТЕЛЬСТВА ЕСТЬ ПРЕДМЕТ, И ЕГО НАДО НАЗЫВАТЬ.**
«Установщик делает то, что написано» и «способность работает» — разные утверждения. Первое закрытие
шага было преждевременным именно поэтому: сухой прогон был зелёным, а связка не работала.

🛑 **Не покрыто:** живой круг на НОВОРОЖДЁННОМ сервере, и правка `bootstrap.sh` подействует лишь
когда Vercel соберёт FES из `fa0faae` — наблюдаемого маркера у коммита нет, проверяет владелец.

🛑 **И ИНСТРУМЕНТАЛЬНЫЙ УРОК, СТОИВШИЙ ТРЁХ ЛОЖНЫХ ИЗМЕРЕНИЙ ЗА СУТКИ:** во всех трёх случаях агент
спрашивал ПОСРЕДНИКА (файл конфига · `/proc/PID/environ` · разметку страницы под замком) вместо
самого предмета. Записано в `/code/development-docs/ANTI-PATTERNS.md`. **Спрашивать надо ту сущность,
чьё поведение проверяешь: службу — через её дверь, способность — вызовом.**

---
## ▶ СЕЙЧАС В РАБОТЕ — ШАГ 114: ОТКАТ 113 И ВОЗВРАТ ТЕРМИНАЛА CLAUDE CODE НА :3600

**План шага целиком — `new-steps/114/114-main.md`. Читать его, а не пересказ здесь.**
Ветки `new-step-114` заведены в `fractera-chatbot-starter` и `fractera-next-starter`.

### 🪦 ШАГ 113 ОТМЕНЁН ЦЕЛИКОМ 2026-09-04 СЛОВОМ ВЛАДЕЛЬЦА

Дословно: «был выбран неправильный инструмент, установили инструмент с которым не получится
работать по подписке, поэтому он нам не нужен». **Claude Agent SDK убран из чата и с экрана бота.**

**114-1 сделан.** Реверт шести коммитов, `reset --hard` не применялся: обе `main` отправлены, и
переписывать отправленную историю запрещено законом репозиториев.

```
fractera-chatbot-starter  eddc3f6   ← revert 9f53935, eec9e4d, 115b918
fractera-next-starter     73f27a4   ← revert b8934a7, aa01732, 2fbab73
```

🔒 **ДОКАЗАТЕЛЬСТВО ОТКАТА — СРАВНЕНИЕ ДЕРЕВЬЕВ, А НЕ СБОРКА.** После реверта код обоих
репозиториев **байт в байт равен состоянию на конец шага 112** — того самого, что собралось с
`BUILD_RC=0`:

```
чат: git diff --stat 1bb1177 HEAD -- . ':(exclude)development-docs'   → пусто
FNS: git diff --cached 109d598                                        → пусто
```

Это сильнее прогона сборки: собираются ТЕ ЖЕ байты, что уже собирались зелёными.

**Миграций у 113 не было** — `lib/db/migrations` не менялся во всём диапазоне `32f46df..41aa57b`.
Ключ никогда не жил в Postgres чата: писатель в FNS правил файл СЛОТА, чат читал его живьём
(`lib/fractera/slot-env.ts`). Откат чистый, база не задета.

**Шаг 112 не задет:** раздел «Стратегия автоматизации», переключатель и запись `automationMode`
остаются. 113 правил `automation-strategy.tsx` поверх — реверт вернул версию 112.

**Итог 113 не удалён, а получил надгробие** (`completed-steps/113-main.md`): там записано знание,
переживающее отмену инструмента — прежде всего то, что **с неверным ключом SDK висит вместо
отказа** (`api_retry` 2·3·4·7·11·22·42·75 с, за 150 с ни ошибки, ни результата).

### ✗ ДЕФЕКТ, НАЙДЕННЫЙ САМИМ ОТКАТОМ, И ОН ШИРЕ ШАГА

`@anthropic-ai/claude-agent-sdk` была объявлена в `package.json`, но **в `pnpm-lock.yaml` её не
было ни до, ни после шага**: `grep -c anthropic pnpm-lock.yaml` = 0 на обеих ревизиях.
🔒 **Замок и объявление разошлись молча.** Сборка на сервере ставила незафиксированную версию, а
`pnpm install --frozen-lockfile` на этом коммите упал бы. Проверять надо оба файла, а не тот, что
правил сам.

### 🛑 ЧТО ЕЩЁ НЕ СДЕЛАНО ПО ОТКАТУ — НАЗВАНО, А НЕ ЗАБЫТО

1. **Сервер по-прежнему несёт код 113.** Откат живёт в ветке; до слияния и сборки на `:3600`
   работает прежняя сборка. Доставка — решение владельца.
2. **Строка `ANTHROPIC_API_KEY=` осталась в `/opt/fractera/app/.env.local`.** Реверт снял и
   писателя, и читателя, но оставленную строку надо стереть на сервере: секрет без единого
   потребителя.
3. **Текст 112 в `automation-strategy.tsx` описывает режим «Claude Agent SDK»** — станет неверным,
   правится подшагом 114-4 вместе с самой поверхностью, а не задним числом.

### ✅ 114-2 СДЕЛАН 2026-09-04 — CLI на сервере, ссылка входа замерена живьём

**`npm i -g @anthropic-ai/claude-code` → `/usr/bin/claude`, версия 2.1.260, `NPM_RC=0`.**
Предпосылки node-pty на месте: node 22.23.2 · `gcc`/`g++`/`make`/`python3` · заголовки node ·
`zsh` в `/usr/bin/zsh`.
**Негативный контроль установки:** ни один из восьми процессов pm2 не перезапустился (`↺` до и после
совпали), двери отвечают `3000→200 · 3001→307 · 3002→307 · 3600→200`.

🔒 **ТРИ НАХОДКИ, И КАЖДАЯ МЕНЯЕТ КОД СЛЕДУЮЩИХ ПОДШАГОВ:**

1. **У CLI есть настоящая подкоманда входа — `/login` внутри интерфейса не нужен.**
   `claude auth login [--claudeai|--console] [--email] [--sso]`, подписка по умолчанию. Мост
   запускает ЕЁ, а не `claude` с последующим набором команды в TUI.
2. **У готовности появился честный прибор.** `claude auth status` печатает JSON:
   `{"loggedIn":false,"authMethod":"none","apiProvider":"firstParty",…}`. Прежний код разбирал
   `{email}` и считал вход по наличию почты; теперь есть булево поле от самого предмета, а
   `authMethod` говорит, ПО ЧЕМУ вошли — подписка или консоль.
3. ✗ **Унаследованная регулярка совпала по домену и разошлась по ГРАНИЦЕ КОНЦА.** Хост тот же
   (`claude.com/cai/oauth/authorize`), `state=` по-прежнему последний параметр. Но класс
   `[A-Za-z0-9_\-]*` после `state=` не останавливается на конце ссылки: прежний код менял перевод
   строки на **пробел** и потом удалял **все** пробелы, и к значению `state` прилипало
   `Pastecodehereifprompted`. Тот же класс съедал `https` второй копии ссылки (её печатает
   гиперссылка OSC-8), из-за чего сторож дубля `indexOf("https://", 8)` не находил ничего.

🛑 **ЭТО ХУДШИЙ ВИД РАСХОЖДЕНИЯ: МОДАЛКА БЫ ОТКРЫЛАСЬ, НО С ИСПОРЧЕННОЙ ССЫЛКОЙ.** Человек получил
бы «ссылка не работает» вместо «модалка не появилась» — отказ, указывающий не туда. **И заметить его
чтением нельзя:** по домену всё совпадало.

**Починено двумя правками, обе из измерения:** основной путь — **OSC-8**, где escape ограничивает
ссылку с обеих сторон и гадать о конце не нужно вовсе; запасной — видимый текст, где переводы строк
удаляются **без подстановки пробела**, а хвост совпадения режется **по соседу в буфере**, а не по
содержимому совпадения.

**Прибор живёт в репозитории:** `development-docs/instruments/114-2-auth-url-extraction.mjs` —
пять позитивных случаев (живой вывод, порванный escape, заворот строки, ссылка вплотную к прозе,
ссылка в конце буфера) и четыре негативных (страница доков, ссылка без `state`, чужой хост, OSC-8 с
посторонней ссылкой). `RC=0`.
🔒 **114-4 обязан ПЕРЕНЕСТИ эту функцию в код, а не переписать регулярку заново** — иначе прибор
сторожит одно, а работает другое.

### 🛑 НАХОДКА КРУПНЕЕ САМОГО ПОДШАГА: НА СЕРВЕРЕ КОД ЧАТА НЕ СООТВЕТСТВУЕТ НИ ОДНОМУ КОММИТУ

`git -C /opt/fractera/chat log -1` говорит **`88d2a7c`** — состояние ДО шагов 112 и 113. А рабочее
дерево несёт их целиком: шесть изменённых файлов и семь неотслеживаемых путей, включая
`lib/fractera/claude-agent.ts` и установленный `node_modules/@anthropic-ai/claude-agent-sdk`.
**Шаги 112 и 113 доставлялись копированием файлов, а не `git pull`.**

1. 🔒 **`git log -1` на сервере — ПОСРЕДНИК, а не предмет.** Он отвечает «какой коммит выкачан», а
   спрашивают его о том, «какой код работает». Ровно тот антипаттерн, что записан в
   `/code/development-docs/ANTI-PATTERNS.md` сутками раньше. **Предмет спрашивается так:**
   `git status --porcelain` рядом с `git log -1`, а лучше — наличие конкретных файлов способности.
2. 🔒 **`pnpm-lock.yaml` разошёлся между сервером и репозиторием** — на сервере он изменён, локально
   `grep -c anthropic` = 0. Замок правился только на машине. Это вторая половина того же дефекта,
   что найден откатом 114-1.
3. 🛑 **ДОСТАВКА ОТКАТА ПОТРЕБУЕТ ОТДЕЛЬНОЙ ОСТОРОЖНОСТИ, И ЭТО ПУНКТ РАБОТЫ, А НЕ ЗАМЕЧАНИЕ.**
   `git merge` не удаляет файлы, о которых git не знает: семь неотслеживаемых путей 112–113
   **переживут слияние** и останутся лежать в боевой сборке. Снимать их надо явно и поимённо.

**Хвост 2 из 114-1 закрыт измерением, а не работой:** строки `ANTHROPIC_API_KEY` в
`/opt/fractera/app/.env.local` **нет** — там только `OPENAI_API_KEY` и `LIGHTRAG_API_KEY`. Владелец
ключ так и не ввёл, стирать нечего.

**Чего в 114-2 нет:** самого входа. `claude auth status` — `loggedIn: false`, и так задумано: вход
делает владелец из браузера через модалку, которой ещё нет. Пробный запуск прерван по таймеру
(`TIMEOUT_RC=124`), состояние входа не изменилось.

### ✅ 114-3 СДЕЛАН 2026-09-04 — мост терминала в процессе чата, замок доказан ОТКАЗОМ

**Коммит `c9ee934`.** Терминал живёт на том же порту, что чат: свой `server.mjs` оборачивает Next —
все запросы уходят `getRequestHandler()`, перехвачен **только** `upgrade` на пути `/pty`, остальные
`upgrade` отдаются `getUpgradeHandler()`. Прежняя команда сохранена как `start:next`.

| Файл | Что |
|---|---|
| `server.mjs` | сервер Next + мост PTY, три режима `init`: `system` · `claude-code` · `claude-login` |
| `lib/fractera/pty-ticket.mjs` | выдача и гашение одноразового билета |
| `app/api/fractera/pty-ticket/route.ts` | дверь: сессия + роль `architect` → билет на 60 с |
| `development-docs/instruments/114-3-pty-lock.mjs` | прибор замка, шесть случаев |

🔒 **БИЛЕТ ЖИВЁТ В `.mjs`, А НЕ В `.ts`, И ЭТО ГЛАВНОЕ РЕШЕНИЕ ПОДШАГА.** Файл читают ДВЕ половины —
дверь внутри сборки Next и `server.mjs` рядом с ней; `server.mjs` TypeScript не исполняет. Написать
проверку билета второй раз значило бы завести **две правды об одном замке**.
🔒 **Хранилище — `globalThis`, и это следствие того же:** импорт из маршрута идёт через сборщик Next,
импорт из `server.mjs` — через загрузчик Node. **Процесс один, а экземпляра модуля два**, и таблица
в области модуля у них была бы разной: билет, выданный дверью, мост бы не нашёл.

🔒 **БИЛЕТ ГАСИТСЯ ДО ЛЮБОЙ ДРУГОЙ ПРОВЕРКИ.** Отказ по режиму, отданный раньше отказа по билету,
рассказал бы непрошеному гостю, какие режимы существуют.

**Доказательство — прибор на НАСТОЯЩЕМ `server.mjs`, не на пересказе. `RC=0`, шесть случаев:**

```
ДВЕРЬ:   403 без роли архитектора · билет архитектору
ЗАМОК:   1008 без билета · 1008 с выдуманным билетом
ЖИЗНЬ:   с настоящим билетом оболочка поднимается и исполняет команду
ПОВТОР:  тот же билет второй раз — 1008
```

🔒 **ТРИ ИЗ ШЕСТИ — НЕГАТИВНЫЕ КОНТРОЛИ.** Замок, не показанный закрытым, не проверен.

#### ✗ Два дефекта, найденных этим же прибором, а не рассуждением

1. **`shellPath()` возвращал `/bin/zsh` или `/bin/bash` без проверки второго**, и на машине
   разработчика node-pty отвечал **`File not found:` с ПУСТЫМ именем файла**. Отказ, не называющий
   предмет, отправляет чинить не туда. Теперь кандидаты меряются `existsSync`, список начинается с
   Windows, а имя оболочки названо в сообщении об отказе.
2. 🛑 **`node-pty` под linux готовой сборки НЕ ИМЕЕТ** — есть только darwin и win32, — и
   компилируется скриптом установки. **А pnpm 10 такие скрипты по умолчанию не запускает**
   (`Ignored build scripts: … node-pty@1.1.0`). Отсюда `pnpm.onlyBuiltDependencies: ["node-pty"]` в
   `package.json`. **Без него модуль ставится «успешно» и не грузится** — на боевой машине это
   выглядело бы как отказ терминала, а не как пропущенная сборка. Остальные заблокированные скрипты
   (`sharp`, `esbuild`, `bufferutil`) намеренно оставлены заблокированными: так было до нас.

🔒 **ОТКАЗ node-pty СДЕЛАН ГРОМКИМ.** Чат поднимается и работает при любом исходе; терминал называет
причину и человеку в ленту, и в журнал. Мягкий перехват здесь означал бы, что сломанное выглядит
исправным.

**Гейты:** `npx tsc --noEmit` `RC=0` · `ultracite` на всех новых файлах чист · оба прибора
перепрогнаны ПОСЛЕ того, как линтер переформатировал живой код, а не до.

🛑 **Чего прибор не проверяет и не притворяется, что проверяет:** боевую сборку и `node-pty` на
linux. Это меряется на самом сервере при доставке.

### ✅ 114-4 СДЕЛАН 2026-09-04 — вкладка терминала и модалка; круг замкнут ГЛАЗАМИ

**Чат `46539f5` · FNS `5a77d98`.** Первая видимая поверхность шага.

| Файл | Что |
|---|---|
| `app/terminal/page.tsx` | маршрут и замок по роли |
| `components/fractera/terminal/xterm-terminal.client.tsx` | обёртка xterm.js |
| `components/fractera/terminal/auth-flow-modal.client.tsx` | модалка активации |
| `components/fractera/terminal/terminal-panel.client.tsx` | сокет, разбор буфера, режимы |
| `lib/fractera/terminal-auth.mjs` | извлечение ссылки — **переехало из прибора 114-2** |

🔒 **ВКЛАДКА ЖИВЁТ ВНЕ ГРУППЫ `(chat)`, И ПРИЧИНА МЕХАНИЧЕСКАЯ.** Раскладка группы монтирует
`<ChatShell />` **рядом** с `children`, а страница разговора возвращает `null`: ленту рисует
раскладка всегда. Внутри группы мы получили бы ленту и терминал на одном экране.

🔒 **ПРИБОР 114-2 ТЕПЕРЬ ИМПОРТИРУЕТ БОЕВОЙ КОД, А НЕ СВОЮ КОПИЮ.** Это исполнение требования,
записанного при закрытии 114-2: прибор со своей копией сторожил бы её, а работала бы другая.

🔒 **ЗАМОК СТОИТ ДВАЖДЫ, И ОБА РАЗА НАМЕРЕННО:** страница проверяет роль, чтобы неуполномоченный
не увидел даже пустой экран; дверь билета — потому что права выдаёт билет, а не страница. Кнопка в
сайдбаре показывается по роли, и в комментарии прямо сказано: **это вежливость, а не замок**, иначе
следующий агент решит, что права проверяются там.

#### ✅ ДОКАЗАТЕЛЬСТВО — ПОЛНЫЙ КРУГ В НАСТОЯЩЕМ БРАУЗЕРЕ, ЧЕТЫРЕ ЗВЕНА

Стенд: заглушка службы входа с ролью архитектора + **настоящий `server.mjs`**.

1. страница открывается, оболочка живая, статус «терминал подключён»;
2. набранное в браузере доезжает до PTY и возвращается обратно (`echo probe-114-4`);
3. на **настоящем выводе `claude auth login`** — том самом, что снят на сервере в 114-2, вместе с
   гиперссылкой OSC-8 — **модалка открылась САМА**, ссылка целая от `https://` до конца `state=`;
4. код из модалки уехал в stdin того же PTY, виден на строке приглашения; окно закрылось само.

#### ✗ ДЕФЕКТ, КОТОРЫЙ НАШЛА ТОЛЬКО КОНСОЛЬ БРАУЗЕРА

Страница спрашивала сессию прямо в своём теле, и Next при включённом `cacheComponents` ответил:

```
Route "/terminal": Uncached data or `connection()` was accessed outside of `<Suspense>`.
This delays the entire page from rendering, resulting in a slow user experience.
```

🛑 **СТРАНИЦА ПРИ ЭТОМ РИСОВАЛАСЬ И РАБОТАЛА.** `tsc --noEmit` — чисто, `ultracite` — чисто, прибор
замка — зелёный. **Дефект был невидим отовсюду, кроме консоли браузера.** Лечение — тот же приём,
которым в раскладке чата обёрнут `SidebarShell`: замок переехал под `<Suspense>`.
🔒 **ОТСЮДА ПРАВИЛО ШИРЕ СЛУЧАЯ: у страницы под `cacheComponents` третья плоскость доказательства —
консоль браузера, и она не заменяется ни типами, ни линтером, ни прибором.**

#### Текст 112 приведён к правде — четыре места, видимых человеку

Карточка режима · подпись переключателя · лид карточки навыков · подсказка раздела на двух языках.
Режим назван **по тому, что делает**, а не именем инструмента; сказано, как это работает на самом
деле — CLI на сервере владельца, вход подпиской через вкладку «Терминал», ключ API не нужен.
🔒 **Ключ режима остаётся `claude`:** он записан в `PLATFORM-CONFIG` живых серверов, и переименование
ключа — миграция, а не текст. Надгробие стоит прямо в файле, чтобы расхождение имени и ключа не
приняли за недоделку.
🔒 **Правила бренда первоисточника соблюдены:** «Claude Code» запрещено в НАЗВАНИИ чужого продукта —
и режим так не назван.

#### 🛑 ЧЕГО Я НЕ ВИДЕЛ ГЛАЗАМИ И НЕ ПРИТВОРЯЮСЬ, ЧТО ВИДЕЛ

1. **Кнопку «Терминал» в сайдбаре.** Лента чата на стенде не поднимается: локального Postgres нет,
   главная страница пуста. Кнопка написана, но **не предъявлена** — проверяется на сервере.
2. **`node-pty` на linux и боевую сборку.** Локально работает сборка под Windows.
3. **Реальный вход по подписке.** Его делает владелец: `claude auth status` — `loggedIn: false`.

**Артефакт стенда, названный, чтобы не искали дефект:** в консоли есть `ClientFetchError` от
`SessionProvider` библиотеки next-auth — у стенда нет `AUTH_SECRET`. К нашему коду отношения не
имеет; сравнить с главной страницей не вышло по причине из пункта 1.

### 🔒 РЕШЕНИЕ ВЛАДЕЛЬЦА 2026-09-04 О ЗАЩИТЕ ТЕРМИНАЛА — ДОСЛОВНО

Агент нашёл измерением, что в режиме по IP служба входа отдаёт роль архитектора **кому угодно**
(`http://213.199.61.7:3001/api/session` без единой куки → `{"email":"demo@local","roles":["architect"]}`),
и порт `3600` смотрит наружу. То есть терминал был бы открыт интернету. Ответ владельца:

> «У нас весь проект работает на незащищённом IP-адресе и предоставляет полный доступ к чтению и
> записи всех баз данных. Это безопасно потому что мы изначально об этом предупреждаем что IP режим
> это промежуточный режим и необходимо использовать защищённый режим, поэтому тебе нет никакого
> смысла в этом режиме что-либо защищать сильнее чем любая другая часть приложения себя защищает.
> Учитывая это просто когда создаёшь имей в виду что после переключения на защищённое соединение
> доступ через порт должен быть отключён так как во всём приложении»

🔒 **ЗАКОН, ГОДНЫЙ ШИРЕ ЭТОГО ШАГА: ЧАСТЬ НЕ ЗАЩИЩАЕТСЯ СИЛЬНЕЕ ЦЕЛОГО.** Терминал наследует ровно ту
защиту, что и слой данных, и панель: в режиме по IP открыт всё, в защищённом — закрыт всё. Особый
замок на одной вкладке был бы не безопасностью, а неравномерностью, о которой никто не помнит.

✅ **ВТОРАЯ ПОЛОВИНА ТРЕБОВАНИЯ УЖЕ ИСПОЛНЕНА ПЛАТФОРМОЙ, И ЭТО ПРОВЕРЕНО ЧТЕНИЕМ КОДА, А НЕ ОБЕЩАНО:**
активация домена зовёт `lockdownFirewall()` (`bridges/app/app/api/config/domain/activate/route.ts:252`),
и тот оставляет только `22/80/443` + `default deny incoming`. Порт `3600` перестаёт отвечать снаружи,
терминал остаётся достижим лишь через nginx на `chat.<домен>/terminal` — по HTTPS и с настоящим
входом. Строить ничего не потребовалось.
✗ **Попутная поправка себе:** сперва я искал вызов по имени `enableFirewall` и, не найдя, чуть не
объявил механизм отсутствующим. Функция зовётся `lockdownFirewall`. **Отсутствие по одному имени не
есть отсутствие способности** — тот же класс, что закон 78-5 про «искать здешний аналог, а не само имя».

---

### ✅ 114-5 СДЕЛАН 2026-09-04 — CLI рождается вместе с сервером; в bootstrap найдены ДВЕ сироты

**FES, ветка `new-step-114`.** Шаг чата стал **десятым**: `chat_agent_cli` ставит
`@anthropic-ai/claude-code` глобально и заводит `/opt/fractera/agent-workspace`.

🔒 **ШАГ МЯГКИЙ, И ЭТО ВЕРНО ПО СУЩЕСТВУ:** сервер без агента-программиста — это работающий сервер и
строка в журнале, а не упавшая установка. Вкладка при этом откроется и честно скажет, чего не хватает.
🔒 **РАБОЧАЯ ПАПКА ЗАВОДИТСЯ ТАМ ЖЕ:** `server.mjs` открывает оболочку в `agent-workspace` и **тихо**
откатывается в свою папку, если её нет. Человек оказался бы в исходниках чата, не понимая почему.

#### ✗ ДВЕ СТРОКИ-СИРОТЫ В ЗАМОРОЖЕННОМ `bootstrap.sh` — ПРОДОЛЖЕНИЕ ОБВАЛА 2026-09-03

Правка того дня вырезала `chat_clone` и `start_chat` вместе с куском файла. Их восстановили — **но
рядом остались две строки, выглядящие как тело шага и стоящие сами по себе**, без `soft_step` перед
ними (строки 916 и 962 до правки).

```
set -e в файле НЕТ (сказано в его же комментарии, строка 144)
строка-сирота → command not found → код 127 → установка молча идёт дальше
```

🛑 **У ПЕРВОЙ СИРОТЫ ЦЕНА БЫЛА НЕ НУЛЕВОЙ ПО ЗАМЫСЛУ:** в ней жила ЕДИНСТВЕННАЯ строка, вычищающая
токен из `.git/config` чата. Она не исполнялась **ни разу**.
✅ **ИЗМЕРЕНО НА ЖИВОМ СЕРВЕРЕ ПРЕЖДЕ, ЧЕМ ПРАВИТЬ: токена там нет** — `CHAT_REPO` публичный и токена
не несёт, чистить было нечего. **Дефект настоящий, ущерб нулевой**, и обе половины названы, чтобы
следующая сессия не искала утечку, которой не было.

🔒 **ПРАВКА ЗАМОРОЖЕННОГО ФАЙЛА ЗАКОННА ИМЕННО ЗДЕСЬ:** удаление строк, которые ДОКАЗАННО не
исполняются, оставляет поведение по умолчанию тождественным, а установка CLI — аддитивное
расширение. Оба условия из `CLAUDE.md` соблюдены.

**Число шагов чата исправлено ВМЕСТЕ с перечислением:** было девять, стало **десять** —
`postgres · db · pnpm · clone · env · deps · build · start · channels_link · agent_cli`.
Тот самый закон, оплаченный в корпусе шесть раз.

**Гейт:** `bash -n lib/bootstrap.sh` чист; четыре строки, похожие на сирот, проверены поимённо —
все четыре продолжения `soft_step … \`, что видно `cat -A`.

🛑 **ЧЕГО НЕТ У ЭТОГО ПОДШАГА:** прогона самого `bootstrap.sh`. Он исполняется только при рождении
сервера, а развёртывание — не моё полномочие. Команда установки при этом **не выдумана**: ровно её я
исполнил на сервере в 114-2 и получил `NPM_RC=0`, `/usr/bin/claude`, версия 2.1.260.

### ✅ ДОСТАВЛЕНО 2026-09-04 — ТЕРМИНАЛ ЖИВОЙ НА БОЕВОМ СЕРВЕРЕ

**Адрес для владельца: `http://213.199.61.7:3600/terminal`.** В интерфейсе — пункт «Терминал» в
левом меню чата.

| Репозиторий | `main` = `origin/main` | Что |
|---|---|---|
| `fractera-chatbot-starter` | **`b8d365a`** | откат 113 · мост · вкладка · модалка · починка режима |
| `fractera-next-starter` | **`5a77d98`** | ключ Anthropic снят, текст 112 приведён к правде |
| `fractera-easy-starter` | **`34cb4a5`** | десятый шаг чата: CLI и рабочая папка при рождении |
| `/code` | — | не задет, не пушится |

**Состояние сервера:** восемь процессов online, семь портов, `3000→200 · 3001→307 · 3002→307 ·
3600→200`, дерево чата чистое на `b8d365a`.

#### Как приводили грязное дерево — поимённо, без разрушающих команд

Классификатор остановил `reset --hard` + `clean -fd` на боевой машине, и это было верно. Сделано
иначе: **сверка каждого файла с `origin/main` до правки**, копия в `/opt/fractera/chat-before-114/`,
затем снятие по именам и `merge --ff-only`.
🔒 **СВЕРКА ОКУПИЛАСЬ ЗНАНИЕМ, А НЕ ТОЛЬКО ОСТОРОЖНОСТЬЮ:** различались ровно три файла, и все три —
те, что меняет 114 (`route.ts`, `package.json`, `automation-mode-selector.tsx`). **Постороннего на
сервере нет** — а до сверки это было предположением.

#### ✗ ГЛАВНАЯ НАХОДКА ДОСТАВКИ: СВОЙ СЕРВЕР ПОДНИМАЛ NEXT В РЕЖИМЕ РАЗРАБОТКИ

Умолчание стояло обратное: `NODE_ENV !== "production"` — то есть при **незаданной** переменной
поднимался dev. `next start` выставляет `NODE_ENV` сам; свой сервер этого не делает, а pm2 запускает
голой командой `pnpm start`.

```
собранная боевая сборка лежала и НЕ ИСПОЛЬЗОВАЛАСЬ
главная страница: 14.5 секунды (компиляция по требованию)
в браузер ехал hmr-client турбопака и next-devtools
гидратация не доходила: вкладка рисовалась и НЕ ОЖИВАЛА — xterm не создавался
```

🛑 **ОТКАЗ ВЫГЛЯДЕЛ КАК УСПЕХ ВЕЗДЕ, ГДЕ БЫЛО ПРИНЯТО СМОТРЕТЬ.** `pnpm build` → `BUILD_RC=0` ·
служба поднялась · слушатель — потомок pm2 · обе страницы → `200` · **консоль браузера чиста**.
✅ **Нашлось по СПИСКУ СЕТЕВЫХ ЗАПРОСОВ** — там лежал `hmr-client`.
🔒 **ЗАКОН, ГОДНЫЙ ШИРЕ СЛУЧАЯ: У СТРАНИЦЫ ЕСТЬ ЧЕТВЁРТАЯ ПЛОСКОСТЬ ДОКАЗАТЕЛЬСТВА — СПИСОК ЗАПРОСОВ.**
Консоль показывает, что СЛОМАЛОСЬ; список запросов показывает, **что за код вообще приехал**. Когда
разметка верна, а поведения нет, вопрос именно второй.

**Починено:** `const dev = process.env.NODE_ENV === "development"` — разработка объявляется ЯВНО,
всё остальное боевое. Измерено после: **0.11 секунды вместо 14.5**, `hmr-client` в разметке — ноль.

#### ✅ ЖИВОЕ ДОКАЗАТЕЛЬСТВО НА БОЕВОМ СЕРВЕРЕ, ГЛАЗАМИ

1. `/terminal` открывается, статус «терминал подключён», приглашение `vmi3542451#`;
2. `claude --version && pwd` → `2.1.260 (Claude Code)` · `/opt/fractera/agent-workspace` —
   **и CLI, и рабочая папка на месте, набрано в браузере**;
3. «Вход по подписке» → CLI напечатал ссылку → **модалка открылась САМА**, ссылка целая.

✅ **`node-pty` СОБРАЛСЯ НА LINUX** — то, чего не проверял ни один локальный прибор:
`INSTALL_RC=0 · NODE_PTY_ЗАГРУЗИЛСЯ · PTY_RC=0 · BUILD_RC=0`.

#### 🛑 ЗА ВЛАДЕЛЬЦЕМ ОДНО ДЕЙСТВИЕ

**Войти по подписке.** Негативный контроль снят при мне: `claude auth status` → `loggedIn: false`,
`authMethod: none`. Вход делает он сам из своего браузера — наш код в обмене не участвует и ключей
не видит.

🔒 **И ОДНО ПРО РЕЖИМ, ЗАПИСАННОЕ ЕГО СЛОВОМ:** пока сервер живёт по IP, терминал открыт так же, как
слой данных и панель. Защищённый режим закроет порт `3600` вместе со всеми (`lockdownFirewall()` при
активации домена), и терминал останется достижим только через `chat.<домен>/terminal` по HTTPS.

### ✅ 114-7 СДЕЛАН 2026-09-04 — состояние терминала переезжало из сессии в сессию

**Чат `d3af1b1`, собрано и доставлено (`BUILD_RC=0`).** Жалоба владельца дословно:

> «когда я нажимаю кнопка вход по подписке то всплывает модальное окно и когда я пробу прохожу и
> возвращаюсь обратно то терминал начинает себя вести неадекватно… я получаю информацию об ошибке
> хотя совершенно то должен был авторизоваться… любое движение на тачпаде приводит к генерации
> множества случайных символов»

🔒 **ОБЕ ЖАЛОБЫ — ОДНА ПРИЧИНА, И ОНА НЕ ТАМ, ГДЕ ИСКАЛИ.**

**Измерено на сервере, а не выведено:**

```
claude auth login   → мышь НЕ включает: 35 секунд ожидания кода, НОЛЬ последовательностей
claude (полный TUI) → ВКЛЮЧАЕТ ?1000h ?1002h ?1003h ?1006h ?1049h ?2004h
                      и НЕ выключает их, когда его обрывают
```

**Значит виноват не вход по подписке, а то, что было ДО него** — и это меняет весь диагноз.

🛑 **МЕХАНИЗМ, И ДЕФЕКТ ЗДЕСЬ МОЙ.** У новой сессии свой PTY, а терминал в браузере **тот же**, и я
нигде его не сбрасывал. Поработав в режиме «Claude Code» и уйдя в другой, человек получал терминал
со **включённым слежением за мышью**: каждое движение по тачпаду уезжает в stdin управляющей
последовательностью, оболочка печатает её как набранный текст — «случайные символы». Та же
последовательность, попавшая в приглашение `Paste code here`, портит код — и вход отвечает ошибкой,
хотя человек всё сделал верно.

🔒 **ПОЧЕМУ «Claude Code РАБОТАЕТ ПРАВИЛЬНО» — ЭТО НЕ ОПРОВЕРЖЕНИЕ, А ПОДТВЕРЖДЕНИЕ.** Внутри живого
TUI слежение за мышью **нужно**, и события съедает он сам. Порча видна только ПОСЛЕ выхода из него.

#### Лечение трёхслойное, и слои разные по смыслу

| Когда | Что делаем | Почему именно так |
|---|---|---|
| открытие новой сессии | полный сброс терминала | PTY новый, состояние старым быть не должно |
| закрытие сокета | возврат режимов, **лента остаётся** | человеку надо прочитать, ЧЕМ кончилось; полный сброс стёр бы этот вывод |
| кнопка «Сбросить» | ручной сброс | признание границы: программа внутри ЖИВОГО PTY способна испортить состояние и не умереть, а заметить это из браузера нечем |

🛑 **БАЙТ ESC СОБИРАЕТСЯ `String.fromCharCode(27)`, А НЕ ПИШЕТСЯ В ФАЙЛ.** Первая версия правки
записала его буквально — работает, но невидимо при чтении и молча теряется при любой перекодировке,
а потерянный превращает лечение в **печать мусора на экран**. Исправлено до сборки.

#### Доказательство — тот самый сценарий владельца, глазами на боевом сервере

1. режим «Claude Code» → TUI отрисовался (мышь включена им же);
2. переключение в «Оболочку» — то есть обрыв TUI, ровно как у владельца;
3. **три движения мыши по терминалу** → приглашение `vmi3542451#` чистое, **ни одного символа**.

#### ✅ И ПОПУТНО ИЗМЕРЕНО ГЛАВНОЕ: ВХОД ВЛАДЕЛЬЦА ВСЁ-ТАКИ ПРОШЁЛ

```
claude auth status → "loggedIn": true · "authMethod": "claude.ai"
```
TUI показывает **«Sonnet 5 · Claude Pro»**. Утром того же дня было `loggedIn: false`.
🔒 **Сообщение об ошибке было правдой о ПОПЫТКЕ и ложью о РЕЗУЛЬТАТЕ** — вход состоялся. Это тот
случай, когда «отказ» и «отказ, о котором сообщили» разошлись, и человек поверил сообщению.

#### 🔒 ЗАЧЕМ ВООБЩЕ РЕЖИМ «ВХОД ПО ПОДПИСКЕ» — ОТВЕТ ВЛАДЕЛЬЦУ, ЗАПИСАННЫЙ ЧЕСТНО

Разница между ним и «Claude Code» — **только в удобстве, и ни в чём больше**. Оба ведут к одному
OAuth-обмену Anthropic:

- «Claude Code» запускает `claude`; невошедшему он показывает свой вход **сам**;
- «Вход по подписке» запускает `claude auth login` — ту же процедуру одной командой, без хождения по
  меню интерфейса.

Режим заведён в 114-4 после замера 114-2, где обнаружилась подкоманда `auth login`: названная дверь
показалась честнее совета «запустите Claude Code и найдите там вход». **Своей способности у него нет
— он не даёт ничего, чего не даёт полный интерфейс.** До 114-7 он был к тому же вреднее: ломался
чаще, потому что его открывали ПОСЛЕ работы в TUI.

🛑 **Оставлен намеренно, и вот довод, а не привычка:** он полезен человеку, который ещё **ни разу не
входил** — ему нужна дверь с именем того, что она делает. Убрать его — решение владельца, не моё;
после починки цена содержания нулевая.

### Следующее действие

**Шаг 114 построен, доставлен и починен по замечанию владельца.** Подписка подключена — измерено (`loggedIn: true`). За владельцем: посмотреть, что режимы больше не переезжают, и решить судьбу режима «Вход по подписке» (разбор выше — своей способности у него нет).

**Не входит в 114:** Гермес и передача данных через терминал.

**Шаг 114 построен и доставлен целиком.** За владельцем — вход по подписке на `http://213.199.61.7:3600/terminal`. После его слова шаг закрывается итогом в `completed-steps/`, и ветки переименовываются в `old-step-114`.

**Не входит в 114 и ждёт его решения:** Гермес (причина в плане шага — рантайма у нас нет ни в одном репозитории) и передача данных через терминал — «предмет отдельного разговора» его словом.





**114-5** — `bootstrap.sh` учится ставить `claude` CLI отдельным `soft_step`: без этого следующий
сервер родится без него и промолчит.

**Доставка** — решение владельца, и она не рядовая: рабочее дерево чата на сервере ГРЯЗНОЕ (112 и
113 доставлялись копированием файлов). `git merge` не удалит семь неотслеживаемых путей — их надо
снять поимённо, иначе отменённый код останется лежать в боевой сборке.

---

## ✅ ПРЕЖНЯЯ РАБОТА — ЭКРАН БОТА, ШАГИ 111–113 (2026-09-04)

> 🔒 **ЗАГОЛОВОК ИСПРАВЛЕН 114-1: ЭТО БОЛЬШЕ НЕ «СЕЙЧАС В РАБОТЕ».** 111 закрыт, 112 закрыт,
> **113 отменён целиком** — состояние работы живёт выше, в блоке шага 114. Две копии одного
> состояния расходятся молча, и этот файл заведён ровно против этого.

### ✅ ШАГ 111 ЗАКРЫТ 2026-09-04 — меню ужалось, карточки стали складными

**FNS `main` = `0d2b1e0`, отправлен · `BUILD_RC=0` на сервере · ветка → `old-step-111`.**
Итог со всеми доказательствами: `completed-steps/111-main.md`.
Убраны разделы «Команды», «Календарь», «Карта»; заведён `_components/settings-card.tsx` — одна
складная карточка на четверых, на `<details>` браузера. Все четыре закрыты по умолчанию.

🔒 **УРОК ШАГА, ГОДНЫЙ ШИРЕ ЕГО: ДЛИННАЯ УДАЛЁННАЯ ОПЕРАЦИЯ И ЕЁ КОД ВЫХОДА РАЗВЯЗЫВАЮТСЯ ФАЙЛОМ.**
Первая сборка осталась без `BUILD_RC`: интерактивная ssh-сессия оборвалась моим таймаутом, процесс на
сервере продолжил работу, а строка кода печаталась ПОСЛЕ `npm` и уехала вместе с сессией. **Лог
выглядел законченным и успешным — то есть отказ измерения выглядел как успех измерения.** Действующий
закон говорил «код выхода брать у `npm`, а не у `tail`» и молчал о том, что канал тоже бывает оборван.
Лечение: `nohup … ; echo $? > /tmp/<имя>.rc`, затем опрос файла.
🔒 **И второе, мельче: файлы слоя архитектора лежат с CRLF.** Скриптовая замена по образцу с LF не
находит НИ ОДНОЙ строки при полностью совпадающем тексте — помощник нормализует концы строк перед
заменой и возвращает их при записи.

---

### 🔴 ШАГ 112 ЗАВЕДЁН 2026-09-04 — «Стратегия автоматизации», кода ещё нет

**РЕШЕНИЯ ВЛАДЕЛЬЦА ЭТОЙ СЕССИИ, ДОСЛОВНО — сохранены раньше кода, потому что от них зависит вся
конструкция:**

> «Делаем вкладку которая будет называться: стратегия автоматизации. На этой вкладке делаем заголовок
> архитектура Fractera предлагает вам на выбор два стандартных способа работы с автоматизации агентов»

> «Claude agent SDK который использует вашу подписку и в целом покрывает расходы для не очень активного
> использования агентной автоматизации. В любом случае требует использование OpenAI для расшифровки
> голоса (при использовании голосового ввода) и для агентного рага и для векторной памяти. Работает на
> стандартных алгоритмах Anthropic управляет автоматизация через создание навыков и MCP»

> «ручное управление процессами автоматизации через ai sdk с моделями OpenAI. Вы сами выбираете
> достаточно ли вам для ваших процессов экстремально дешёвых моделей в комбинации с дорогими на
> различных процессах. Это позволит более тонко разработать архитектуру и при необходимости
> масштабировать её на промышленное использование с большим числом пользователей»

> «по умолчанию показываем переключатель в состоянии режим Cloud агент SDK активен»

> «Если пользователь переводит режим в противоположное состояние то открывается таблица настройки…
> заголовок в этой таблице будет настройка ручного конвейера автоматизации. Первый аккордеон разбор
> входящего сообщения – агенты AI SDK»

> «В случае если режим по умолчанию с Cloud агент SDK то показываем заголовок настройка
> автоматического режима. Ниже два аккордеона каждая из которых будет иметь вложенные аккордеоны
> первый это скилы второй это MCP пока нигде ничего нет»

🔒 **ГЛАВНОЕ РЕШЕНИЕ, МЕНЯЮЩЕЕ АДРЕС ПРАВДЫ. Дословно:**

> «в стратегии автоматизации нужно учесть модификацию попадающего списка моделей внутри самого чата
> внутри области ввода текста там сейчас ассортимент модели OpenAI но теперь это становится
> переключателем двух режимов: первый режим Cloud агент SDK, второй режим AI SDK с openai. Эта же
> настройка будет пробрасывать в тот самый CONFIG, будет по факту первичный и основной, а настройка
> которая у нас находится в настройках Telegram бот это просто дополнительный механизм управления,
> дверь, в то время как ядро живёт порт 3600»

🔒 **ОТСЮДА: ХОЗЯИН НАСТРОЙКИ — ЧАТ (`:3600`), А ЭКРАН АРХИТЕКТОРА — ВТОРАЯ ДВЕРЬ К ТОМУ ЖЕ
ЗНАЧЕНИЮ.** Это разворот прежнего умолчания: раньше настройка заводилась на экране архитектора, и чат
её не знал. Значит работа двухрепозиторная, и `automationMode` обязан быть ОДНИМ ключом в одном
хранилище, а не двумя правдами. Тот же закон, которым закрыт шаг 109: одна дверь, много звонящих.

🔒 **ВЫБОР МОДЕЛИ УХОДИТ ИЗ ЧАТА, И ПРИЧИНА АРХИТЕКТУРНАЯ, А НЕ КОСМЕТИЧЕСКАЯ. Дословно:**

> «Именно в настройках которые мы сейчас будем программировать, там где у нас ручной режим, мы
> позволим пользователю самостоятельно определить для каких агентов он выберет слабую модель а для
> каких сильную, поэтому внутри чата переключатель модели нецелесообразно использовать — один выбор ко
> всем агентам. Другими словами переходим к персональным настройкам каждого агента, а у нас будет
> буквально для каждого элемента реестра признаков создан свой агент»

🔒 **ЭТО ЖЕ МЕНЯЕТ ПРЕДМЕТ РАЗВИЛКИ ШАГА 106.** «Дешёвая модель или дорогая» перестал быть одним
вопросом на весь конвейер: моделей столько же, сколько агентов, а агентов — сколько элементов реестра
признаков. Замер 106-3 меряет теперь не выбор для продукта, а **метод выбора для одного агента**.

🪦 **ПЛАН ШАГА 108 ОТМЕНЁН ЭТИМ ЗАКАЗОМ, И ЭТО СКАЗАНО ВСЛУХ.** Он ставил переключатель В
ЗАБЛОКИРОВАННОЕ положение «ручной режим», а автоматический не трогал вовсе. Теперь наоборот: по
умолчанию активен Cloud Agent SDK, и оба положения открывают свои настройки. Позднее слово владельца
сильнее; требование 108 «место под будущее одной секцией» шагом 112 исполняется целиком.

**Место раздела выбрано владельцем интерфейсом выбора:** отдельный раздел левого меню бота между
«Настройками» и «Паспортом», а не шестая карточка внутри «Настроек».

---

### 🔴 ИЗМЕРЕНО ПЕРВОИСТОЧНИКОМ 2026-09-04 — ПОДПИСКА ЧУЖИМ ПРОДУКТАМ ЗАПРЕЩЕНА АНТРОПИКОМ

**Источник, названный владельцем:** `https://code.claude.com/docs/en/agent-sdk/overview`.
Прочитан целиком, не пересказом. Четыре факта, каждый решает конструкцию:

🛑 **ГЛАВНОЕ, И ОНО ПРОТИВОРЕЧИТ ЗАМЫСЛУ ВЛАДЕЛЬЦА. Дословно из документации:**

> «Unless previously approved, Anthropic does not allow third party developers to offer claude.ai
> login or rate limits for their products, including agents built on the Claude Agent SDK. Use the
> API key authentication methods described in the Quickstart instead.»

**То есть «использует вашу подписку» — ровно то, что Антропик запрещает предлагать сторонним
продуктам без предварительного согласования.** Прежний замысел владельца («подключить сессию
Anthropic Agent SDK по подписке через браузер», 2026-09-04) документацией НЕ поддержан. Законный путь
один: ключ API с потокенной оплатой. 🔒 **Значит фраза о подписке в описании вкладки была бы обещанием
механизма, которого нет, — и в чужих терминах ещё и нарушением.** Развилка «просить согласования у
Антропика» существует и является решением владельца, а не правкой.

🔒 **ИМЯ — `Claude Agent SDK`, НЕ `Cloud`.** Поправка владельца 2026-09-04: «Cloud Agent SDK
incorrect». Плюс правила бренда в той же странице: **разрешено** «Claude Agent» (предпочтительно для
выпадающих меню) и «Claude»; **запрещено** «Claude Code» и «Claude Code Agent» в названии чужого
продукта. Наша вкладка называет режим «Claude Agent SDK» и не притворяется Claude Code.

✅ **НАВЫКИ И MCP ПОДДЕРЖАНЫ — замысел владельца про реестр признаков как навыки механикой SDK не
запрещён.** Дословно: skills, commands и memory «Load automatically from your project's `.claude/`
and from `~/.claude/`, same as Claude Code»; MCP — «Connect external tools and data sources via the
Model Context Protocol». Есть также подагенты, хуки, права, сессии и плагины.

✅ **РАНТАЙМ — TypeScript и Python, «only».** Чат на `:3600` — TypeScript, то есть SDK ложится в него
библиотекой в НАШЕМ процессе, без песочницы провайдера. 🔒 **Отсюда прямое следствие для действующего
закона о `SKILL UPLOADS`:** тот запрет написан про песочный контейнер НА СТОРОНЕ ПРОВАЙДЕРА, не
видящий нашего слоя данных; здесь процесс наш, и запрет к этому случаю не относится. Разрешение при
этом всё равно не выведено — навыки Agent SDK ещё не пробовали.

🔒 **РЕШЕНИЕ ВЛАДЕЛЬЦА, СНИМАЮЩЕЕ ОБА МОИХ ВОПРОСА. Дословно 2026-09-04:**

> «Сейчас она в чат ни в одном из режимов ничего делать не умеет поэтому глупо думать что это
> переключение может что-то сломать. Пусть оно переключается но ни на что не влияет пока»

**Значит:** переключатель — чистое состояние, поведения за ним нет ни в одном положении; ответ чата
идёт как сейчас, модель берётся из `OPENAI_TEXT_MODEL` (существующая настройка, новой правды о модели
не заводится). Вопрос «что делает чат в режиме Claude Agent SDK» закрыт: **ничего, и это сказано на
экране словами.**

---

## ▶ ПРЕЖНЕЕ СОСТОЯНИЕ СЕССИЙ 2026-09-03

**Ничего не строится. Сессия 2026-09-03 (вечер) была ЧТЕНИЕМ ДОКУМЕНТАЦИИ по прямому указанию
владельца: «задача этой сессии только проработать документацию без программирования».** Кода не
написано ни строки — это выполнено, а не забыто.

### Что произведено и куда легло

| Репозиторий | Коммит | Что |
|---|---|---|
| `fractera-chatbot-starter` | **`5c55f01`**, `main == origin/main`, отправлено | `development-docs/AI-SDK-TOOL-SELECTION-RESEARCH.md` — разбор документации `ai@7.0.89` с адресом в документации на каждое утверждение |
| `fractera-next-starter` | **`70d1011`**, `main == origin/main`, отправлено | `development-docs/PASSPORT.md`, **новый раздел 9** — цель, конвейер, таксономия входящих; прежняя подсветка снята по § 9.5 |
| `/code` | **`4d9e29c`**, не пушится | отмена устаревшего запрета на push + урок в `ANTI-PATTERNS.md` (`084f843`) |

🔒 **ВХОД ДЛЯ СЛЕДУЮЩЕЙ СЕССИИ — ДВА ДОКУМЕНТА, И ОНИ ДОПОЛНЯЮТ ДРУГ ДРУГА, А НЕ ПОВТОРЯЮТ:**
`PASSPORT.md` § 10.1–10.10 (замысел, конвейер, долг) и `AI-SDK-TOOL-SELECTION-RESEARCH.md`
(доказательства и адреса в `node_modules/ai/docs/`). Владелец заказал их именно так: «чтобы следующая
сессия могла полностью автономно проложить эту автоматизацию».

### Решения владельца этой сессии — дословно

> «выбрать подходящего агента для той или иной задачи: например человек написал измени цвет темы на
> оранжевый — наш чат должен вызвать агента у которого способность изменять настройки дизайн CONFIG»

> «каждый агент пусть работает на модели мини и выполняет свою рутинную задачу по инструкции»

> «чтобы с одной стороны нить общения оставалась целостной, с другой стороны чтобы она [не] была
> засорена мусорными данными»

> «библиотека чата» = AI SDK — имя введено владельцем, записано в федеральный `GLOSSARY.md`.

**Правило работы, установленное им же:** вопрос → агент определяет страницы документации → **кладёт
их в контекст ЦЕЛИКОМ, не экономя**; после каждой пачки называет остаток контекста вслух.

### Дорогие факты — НЕ добывать заново

Все с адресами лежат в файле исследования; здесь только то, что решает направление:

- **надёжность даёт КОНСТРУКЦИЯ, а не класс модели** — на верхних ступенях принуждения выбор делает
  наш код, и mini влияет лишь на заполнение аргументов;
- **«не более пяти инструментов» — совет по промпту, а не предел**; предел снимается `activeTools`;
- **«навыков» для `ToolLoopAgent` в SDK НЕТ** — они только у `HarnessAgent`, то есть проброс в чужой
  харнесс (Claude Code, Codex, Pi) в песочнице;
- **много агентов — то, чего `callOptions` позволяет ИЗБЕЖАТЬ** (`05-configuring-call-options:19`);
- **code mode экономит не на выборе, а на числе круговых рейсов** — одна программа вместо N;
- **реранкер многоязычный есть, но провайдеры Cohere/Bedrock/Together — второй ключ**;
- **эмбеддинги у нас уже стоят**: `text-embedding-3-small`, 1536, `sqlite-vec`;
- **неподдержанная настройка провайдером игнорируется МОЛЧА**; единственный прибор — `result.warnings`.

### ✗ Два дефекта, найденных ИЗМЕРЕНИЕМ кода (не заведены шагами)

1. **У браузерного пути чата нет окна истории.** `getMessagesByChatId` без `LIMIT`; браузер шлёт всё,
   Telegram режет `slice(-20)`. Обрезан второстепенный путь, безлимитен главный.
2. **`convertToModelMessages` зовётся без `tools`** (`app/(chat)/api/chat/route.ts`). Безвредно, пока
   нет субагентов; с первым субагентом изоляция контекста перестанет работать на следующем ходу.

Оба описаны в `PASSPORT.md` § 10.7 и в файле исследования § 8–9.

### 🛑 Метод: один класс ошибки сработал ДВАЖДЫ за день

Утром агент прочёл в `/code/current-steps.md` объявление о переезде учёта и **не пошёл по названному
адресу** — предложил владельцу как новую работу шаг 97, закрытый накануне. Вечером прочёл устаревшие
копии факта о доступе и **дважды объявил «push заблокирован»**, хотя `BACKLOG` 17 был закрыт утром.

🔒 **Общее у обоих: доверие тексту там, где команда даёт правду за секунду.** Оба урока записаны —
первый в `/code/development-docs/ANTI-PATTERNS.md`, второй надгробиями в `CLAUDE.md` и
`/code/current-steps.md`.

### ✅ ШАГ 106 ЗАВЕДЁН И СОГЛАСОВАН 2026-09-03 — КОДА ЕЩЁ НЕТ

**План: `new-steps/106/` — `106-main.md` плюс четыре ТЗ (`106-1` … `106-4`).**
**Этап 9 пройден, ответ владельца дословно: «Подтверждаю».**

| Подшаг | Что делает | Состояние |
|---|---|---|
| 106-1 | список намерений из живых фраз `inbox.jsonl`, с обязательными `none` и `example` | не начат |
| 106-2 | прибор замера: фраза → намерение → уверенность, печатает `warnings` | не начат |
| 106-3 | прогон через дешёвую и дорогую модель, три метрики, промахи глазами | не начат |
| 106-4 | сужение эмбеддингами: recall@10 и recall@5 по реестру признаков | не начат, **независим — может идти первым** |

🔒 **Условие закрытия шага: 106-3 И 106-4 оба закрыты.** Одно число из двух оставляет развилку
«дешёвая модель или дорогая» открытой наполовину.

🔒 **Адрес работы:** план и учёт в `main`, **код шага — на ветке `new-step-106`**. Причина в
`106-main.md`: репозиторий держит и код, и учёт, а план, оставленный на ветке, не виден сессии в
`main`.

🛑 **Главное ограничение шага, названное ДО работы:** `inbox.jsonl` — личная переписка владельца.
Фразы не уезжают ни в git, ни в отчёты; внутрь репозитория попадают **только числа и обезличенные
образцы промахов**.

### ▶ 106-4 НАЧАТ 2026-09-03 — разведка сделана, замер упёрся в доступ

**Ветка `new-step-106` создана.**

#### ✗ ДОРОГОЙ ФАКТ, ДОБЫТЫЙ ПЕРВЫМ ЖЕ ИЗМЕРЕНИЕМ: ВСТРОЕННЫХ ПРИЗНАКОВ **36**, А НЕ 25

Измерено не грепом, а **вызовом порождающего кода** (приём из 81-1): `builtinFacts()` собран
`esbuild --bundle --platform=node --format=esm --alias:@=.` и исполнен.

```
ВСЕГО ВСТРОЕННЫХ: 36
  initiator = 4 · material = 9 · intent = 8 · entity = 6 · destination = 3 · field = 6
без howToFind: 0 · средняя длина howToFind: 69 символов
```

🛑 **«25» СТОИТ СЕГОДНЯ В ТРЁХ МЕСТАХ И ВЕЗДЕ НЕВЕРНО:** федеральный `CLAUDE.md`, `PASSPORT.md`
стартера и `AI-SDK-TOOL-SELECTION-RESEARCH.md`. Прежняя раскладка была `2+8+6+3+6 = 25`; с тех пор
группа «чем пришло» выросла с 2 до 9 (девять родов, они же перечислены в паспорте § 5.3.2), и
добавилась группа `initiator` из 4 — **ни одна правка не сдвинула число**.
🔒 **Это ровно тот класс, о котором корпус написал закон четыре раза за неделю.** Число исправляется
ВМЕСТЕ с перечислением — при закрытии шага, одной правкой на каждый файл.

**Что это меняет в самом замере:** «первая десятка» — это не 40 % реестра, как записано в ТЗ 106-4, а
**28 %**; recall@5 — 14 %. Вывод формулируется относительно 36, и число названо в итоге.

#### 🛑 ЧЕМ ЗАБЛОКИРОВАН ЗАМЕР — НАЗВАНО ТОЧНО

1. **Ключа OpenAI нет ни в одном локальном окружении.** Проверены `fractera-chatbot-starter/.env.local`
   (файла нет вовсе), `fractera-next-starter/.env.local`, `ai-workspace/bridges/app/.env.local` — ключа
   нет. Без него эмбеддинги не посчитать, а **подставлять вместо замера нечего**: замер без настоящего
   вызова не замер (ТЗ 106-2, раздел 4).
2. **Живые фразы лежат только на сервере** (`inbox.jsonl` службы каналов), а SSH-обёртка в этой сессии
   **остановлена гейтом окружения**. Гейт разумный — это боевая машина; обходить его запрещено.

**Что уже готово и переживёт паузу:** прибор извлечения реестра (15 строк TypeScript + одна команда
`esbuild`) воспроизводится за минуту; его результат — 36 записей с `title`, `description`, `howToFind`.

### ✅ ГРАФ ЗНАНИЙ ПОЧИНЕН НА СЕРВЕРЕ 2026-09-04 — попутная работа по слову владельца

**Доступ к серверу владелец дал в этой сессии** («доступ к серверу разрешаю»), задача — «чини граф и
заводи 107, потом возвращайся к 106-4».

🔒 **ДОРОГОЙ ФАКТ: ГРАФ НЕ ЧИТАЕТ `OPENAI_API_KEY`.** LightRAG читает **`LLM_BINDING_API_KEY`** и
**`EMBEDDING_BINDING_API_KEY`**; обе были пусты, а переменной `OPENAI_API_KEY` в его файле не было
вовсе. Писатель экрана бота (`lib/architect/openai-key.ts`) пишет туда именно `OPENAI_API_KEY` — то
есть **«Сохранить ещё раз» позеленило бы плашку и оставило граф слепым**. Причина расхождения названа
владельцем: раньше он вводил ключ в административной панели (её писатель знал верные имена), теперь —
на экране Telegram-бота.

**Сделано:** в `services/rag/.env` вписаны две настоящие переменные, `fractera-rag` перезапущен.
`OPENAI_API_KEY` туда намеренно **не** заведена — покупать зелёный цвет плашки запрещено.
Резервная копия: `/opt/fractera/services/rag/.env.before-106`.

**Доказано двумя плоскостями:**
1. *файл* — обе переменные из «пустая» стали «ЗАПОЛНЕНА»; негативный контроль: `OPENAI_API_KEY`
   в файле отсутствует, проверено;
2. *способность* — `POST /documents/text` → через 10 с `{"processed":1,"failed":0}`; затем
   `DELETE /documents` → `{"all":0}`. Граф возвращён ровно в прежнее состояние (был пуст).
   Дойти до `processed` без живого вызова встраивания нельзя.

🛑 **Названо вслух: доказательство через окружение процесса НЕДОСТИЖИМО.** `/proc/PID/environ`
показывает окружение на момент запуска и не отражает то, что процесс подгрузил из `.env` сам —
отсутствие там ничего не доказывает. Поэтому вторая плоскость функциональная, а не «посмотрели в
переменные».

🛑 **СДЕЛАННОЕ РУКАМИ ЖИВЁТ НА ОДНОЙ МАШИНЕ.** Новый сервер родится с тем же дефектом.
**Источник чинит шаг 107** — заведён: `new-steps/107-openai-key-must-reach-lightrag-by-its-own-variable-names.md`.
Короткий шаг без подшагов, согласования этапа 9 не требует по границе владельца.

**Попутно измерено и полезно дальше:** `inbox.jsonl` — **62 строки** (замер 106 пойдёт на 62, а не на
100) · Node на сервере **v22.23.2** → требование code mode (22+) выполняется · слой данных читает ключ
из файла слота и **готов считать эмбеддинги** — 106-4 не заблокирован.

### ✅ 106-4 ЗАКРЫТ 2026-09-04 — коммит `945ea81`, итог `completed-steps/106-4.md`

**Ответ отрицательный там, где он был нужен, и это ценнее положительного.**

```
по всему реестру (36):  recall@1 39% · recall@5 53% · recall@10 56%   ← не работает
негативный контроль:    настоящие запросы 0.449 · мусор 0.21–0.26     ← прибор различает
внутри уровня:          initiator 100% (случайно 25) · field 83% (17) · material 78% (11)
                        intent 12% (12) · entity 17% (17) · destination 33% (33)
```

🔒 **РАБОТАЮТ УРОВНИ О ФОРМЕ, НЕ РАБОТАЮТ УРОВНИ О СМЫСЛЕ, И РАСКОЛ ИДЕАЛЬНО ЧИСТЫЙ.** Три уровня
далеко выше случайности, три — ровно на ней. Это не про число кандидатов, а про природу: «голос» и
«фотография» объективно разные вещи, а «намерение» и «сущность» требуют суждения.

🔒 **ОТСЮДА ПРАВКА КОНВЕЙЕРА ПАСПОРТА § 10.3, И ОНА СУЩЕСТВЕННАЯ.** Ступень 2 (бесплатное сужение
эмбеддингами) снимает работу **только там, где ответ и так известен из метаданных** — служба каналов
уже знает род вложения. Там, где ответ неизвестен, она не помогает вовсе. **Смысловые уровни идут не
на ступень 2, а сразу на ступень 3, к классификатору.** Паспорт правится при закрытии шага.

🛑 **ЖИВОГО КОРПУСА НЕ СУЩЕСТВУЕТ, И ЭТО ГЛАВНОЕ ДЛЯ 106-1…106-3.** `tgdesk_messages` 0 ·
`vectors` 0 · `fact_registry` 0 · `inbox.jsonl` — один день (сервер рождён 02:18 того же дня), 27
уникальных фраз, и это **журнал тестирования, а не работы**: «Привет», «Ок», «2+3», «44:22». Рабочих
просьб среди них три.

**Значит 106-1 (список намерений из живого потока) в нынешнем виде НЕ ИСПОЛНИМ** — собирать список из
27 тестовых фраз значило бы выдумать его, прикрывшись данными. Решение за владельцем: дать фразы
самому · пользоваться ботом неделю и мерить потом · либо строить список из реестра и мерить
различимость, как сделано здесь.

### ✅ ШАГ 107 ЗАКРЫТ 2026-09-04 — ключ доезжает до графа ЕГО именами

**FNS: `fe575cb` → влит в `main` = `5b05b59`, отправлен, собран на сервере (`BUILD_RC=0`,
`:3000 → 200`, слушатель — потомок pm2).** Итог: `completed-steps/107-main.md`. План удалён тем же
коммитом, восстановим из git. Ветка → `old-step-107`.

🔒 **ЗАКОН ШАГА: ИМЯ ПЕРЕМЕННОЙ ПРИНАДЛЕЖИТ ПОТРЕБИТЕЛЮ.** LightRAG читает `LLM_BINDING_API_KEY` и
`EMBEDDING_BINDING_API_KEY`; писатель теперь пишет все имена потребителя, а «задан» значит
«заполнены ВСЕ», а не «хотя бы одна».

✅ **ТРЕТЬЕ ДОКАЗАТЕЛЬСТВО ПОЛУЧЕНО ГЛАЗАМИ ВЛАДЕЛЬЦА 2026-09-04.** Строка «Агентный RAG» на
`/{lang}/architect/telegram?section=settings` стала «задан». Подтверждено им дословно:
«confirmed early, fix as succes and close». Долгов за владельцем по шагу 107 не осталось.

🛑 **ДОЛГ, ОСТАВЛЕННЫЙ СОЗНАТЕЛЬНО: чат пишет ключ ТОЛЬКО в файл слота** — ни слою данных, ни графу.
Ожидание владельца «ключ перехватывается в любом месте» выполняется на две трети. **Чинить копией
писателя в третий раз запрещено** — так и родился этот дефект. Правильная форма: одна дверь, три
звонящих. Отдельный шаг.

### ✅ ШАГ 109 ЗАКРЫТ 2026-09-04 — одна дверь для ключа, три звонящих

**Все четыре подшага закрыты. Итоги:** `completed-steps/109-1.md` и `109-main.md`.
План удалён тем же коммитом, восстановим из git.

| Репозиторий | `main` | Что |
|---|---|---|
| `ai-workspace` | `4ca6c6b` | дверь в службе данных + панель звонит |
| `fractera-next-starter` | `75362e7` | экран архитектора звонит + прибор реестра |
| `fractera-chatbot-starter` | `e7793ef` | чат звонит |

Всё собрано на сервере: `BUILD_RC=0` у панели, слота и чата; `:3000` 200 · `:3002` 307 · `:3300` 401 ·
`:3600` 307; дверь отвечает `configured=true` по всем трём потребителям.

🔒 **ЗАКОН ШАГА, ГОДНЫЙ ШИРЕ ЕГО:** знание о чужой службе — её имена переменных, её файлы — живёт в
ОДНОМ месте платформы, и это место **не там, где на кнопку нажимают**. Три экрана, знавшие одно и то
же по-своему, — не три реализации, а три возможности разойтись.

✅ **ПОСЛЕДНЕЕ ДЕЙСТВИЕ ВЛАДЕЛЬЦА ВЫПОЛНЕНО — 109 ЗАКРЫТ ЦЕЛИКОМ, 2026-09-04.** Ключ сохранён на
экране, **все три строки** (приложение · векторная память · агентный RAG) показали «задан». Провод
между звонящим и дверью — адрес и секрет у каждого — проверен настоящим нажатием, а не подменён
легкодоступным. Слово владельца дословно: «confirmed early, fix as succes and close».
Полная запись — `completed-steps/109-main.md`, «Доказательство 3».

🔒 **УРОК ДОРОЖЕ САМОГО ФАКТА: доказательство было получено и записано в итог шага, а этот файл
продолжал числить его за владельцем.** Две копии одного состояния разошлись молча — ровно тот класс
отказа, ради которого файл и заведён. **Пункт «за владельцем» гасится в ОБОИХ адресах одной
операцией**, а не в том, который под рукой. ✗ оплачено этой самой строкой: владельцу пришлось
отвечать «confirmed early» на вопрос, ответ на который уже лежал в `109-main.md`.

### Следующее действие

**Шаг 110 закрыт 2026-09-04** — дефект, который выстрелил бы на каждом следующем развёртывании,
вылечен в источнике. **Долгов за владельцем не осталось ни одного.** Открытых работ ровно две, в
любом порядке:

- **108** — секция «Настройка конвейера» с заблокированным автоматическим режимом; план заведён,
  код в `fractera-next-starter`, ветки `new-step-108` ещё нет. **От владельца не зависит.**
  🛑 Якорь размещения в плане («ниже карточки реестра признаков») стал неоднозначен после 109: под
  ней теперь стоит вторая карточка `ToolsRegistry` (`_components/telegram-settings.tsx:72–75`).
  Место секции называется ДО работы, а не выбирается по ходу;
- **106-1** — ждёт решения владельца (A/B/C); без него стоят и 106-2, и 106-3. Разбор ниже.

🛑 **ПОПУТНО ПЕРЕПРОВЕРЕНО В КОДЕ 2026-09-04, ОБА ДЕФЕКТА ЖИВЫ:** `getMessagesByChatId` без `LIMIT`
(`lib/db/queries.ts:257`) и `convertToModelMessages` без `tools` (`app/(chat)/api/chat/route.ts:261`).
Шагами не заведены; второй выстрелит с первым же субагентом.

🛑 **УСТАРЕЛ НЕ ПЛАН, А ПАСПОРТ:** `PASSPORT.md` § 10.3 всё ещё описывает ступень сужения
эмбеддингами так, как её опроверг замер 106-4. Правка отложена «до закрытия шага 106», а шаг стоит на
106-1 — значит документ замысла вводит в заблуждение **прямо сейчас**, и следующая сессия читает
его первым.
### 🔴 106-1 — ЖДЁТ РЕШЕНИЯ ВЛАДЕЛЬЦА, И ЭТО ПЕРВОЕ, О ЧЁМ ЕГО СПРОСИТЬ

**Владелец 2026-09-04 попросил не потерять этот пункт: «remember about 106-1».**

**Что заблокировано.** 106-1 — список намерений, собранный **из живого потока**. 106-2 и 106-3
зависят от него, то есть без решения стоит **три четверти шага 106**.

🛑 **ПОЧЕМУ НЕ СДЕЛАНО: ЖИВОГО КОРПУСА НЕ СУЩЕСТВУЕТ, И ЭТО ИЗМЕРЕНО.** `tgdesk_messages` 0 ·
`vectors` 0 · `fact_registry` 0 · `inbox.jsonl` охватывает ОДИН день (сервер рождается заново), и
27 уникальных фраз в нём — **журнал тестирования, а не работы**: «Привет», «Кто ты», «Ок», «hola»,
«2+3», «44:22», «ты видишь это сообщение». Настоящих рабочих просьб среди них три.

🔒 **СОБРАТЬ СПИСОК НАМЕРЕНИЙ ИЗ ЭТОГО = ВЫДУМАТЬ ЕГО, ПРИКРЫВШИСЬ ДАННЫМИ.** Агент отказался это
делать намеренно, а не забыл. Отказ — результат подшага, и он предусмотрен его же ТЗ: «если фраз
окажется меньше, чем нужно, это результат подшага, а не провал — он называется числом, и владелец
решает».

**Три пути, выбор за владельцем — предъявить ему интерфейсом выбора:**

| | Что делаем | Цена |
|---|---|---|
| **A** | владелец даёт 30–50 своих настоящих просьб сам | быстро; честнее живого лога, потому что лог отражает тестирование, а не работу |
| **B** | пользоваться ботом по-настоящему неделю, мерить потом | самый достоверный корпус, но ждать |
| **C** | строить список из реестра и мерить различимость | не требует владельца; измеряет реестр, а не употребление — так сделан 106-4 |

🛑 **И ВАЖНОЕ ПРО КОРПУС, ЧТО БЫ НИ ВЫБРАЛИ:** `inbox.jsonl` — личная переписка владельца. Фразы не
уезжают ни в git, ни в отчёты; внутрь репозитория попадают только числа и обезличенные образцы.

🛑 **Ветка `new-step-106` не влита в `main`** — там закрыт только 106-4.

Когда он вернётся, первое по порядку названо в `PASSPORT.md` § 10.8 и **не является кодом**: прогон
на 100 живых фразах из `inbox.jsonl` службы каналов — попадает ли mini в список намерений и попадает
ли верный признак в первую десятку при сужении эмбеддингами. От этого числа зависит вся развилка
«дешёвая модель или дорогая», и спорить о ней до замера бессмысленно.
🛑 **Списка намерений пока не существует** — он первый предмет работы, без него замерять нечего.

---

## 🪦 СТРАТЕГИЯ ВЛАДЕЛЬЦА ПО АРХИТЕКТУРЕ ЧАТА — ОТМЕНЕНА ЦЕЛИКОМ 2026-09-05 (ревизия, шаг 116)

🪦 **ЭТОТ РАЗДЕЛ БОЛЬШЕ НЕ ЗАКОН, А ИСТОРИЯ. НЕ ПЛАНИРОВАТЬ ПО НЕМУ.** Он описывает выбор между
конвейером на моделях OpenAI и агентом Anthropic — выбор, которого больше нет: путь к ИИ один,
Telegram → Claude Code. Раздел «Стратегия автоматизации» и его код удалены той же ревизией.
Оставлен целиком, потому что удалённый бесследно замысел следующая сессия воскрешает по памяти и
как новость. Действующее — в блоке «РЕВИЗИЯ 2026-09-05» в начале этого файла.

**Замысел целиком лежит в `fractera-next-starter/development-docs/PASSPORT.md`, § 10.1–10.12** — там
он тоже помечен надгробием.

### Два режима, и переключатель между ними — настройка, а не выбор в коде

| Режим | Чем работает | Состояние |
|---|---|---|
| **ручной** | тонко настроенный конвейер на комбинации **дешёвых и средних моделей OpenAI** | действует |
| **автоматический** | **Anthropic Agent SDK** — один установщик проходит весь цикл без нашей декомпозиции | 🔒 **заблокирован намеренно**, место занято под будущее |

Слово владельца 2026-09-04, дословно: «минимально хардкор запланировать секцию в настройках, чтобы в
будущем там поставить активацию Anthropic агент SDK, ниже в режиме поставить переключатель в
заблокированное положение ручной режим, а режим автоматически через Anthropic агент SDK пока не
трогать, и продолжать работу в ручном режиме чтобы тестировать два состояния: первый шаг при помощи
маршрутизатора на функциях либо первый шаг при помощи модели OpenAI».

🔒 **МЕСТО СТОИТ ОДНУ СЕКЦИЮ, ПЕРЕДЕЛКА СКЕЛЕТА ЗАДНИМ ЧИСЛОМ — НЕДЕЛИ.** Поэтому переключатель
ставится сразу, а второй режим не строится. Это **шаг 108**, план заведён.

### Что именно сравнивается в ручном режиме

**Маршрутизатор на функциях** (решает наш код, ноль вызовов модели) **против модели OpenAI**
(решает классификатор). Ни одно не объявлено верным заранее — настройка заводится ради их сравнения.

🔒 **ПОЧЕМУ ВООБЩЕ НАСТРОЙКА, А НЕ ВЫБОР В КОДЕ.** Измерение 106-4 показало, что верная конструкция
ступени сужения была угадана неверно. Настройка — способ держать гипотезу **проверяемой в бою**, а не
хоронить догадку в коде. Это продолжение действующего паттерна: `OPENAI_TEXT_MODEL` уже настройка.

### Замысел владельца про Agent SDK — его слова, не мой пересказ

> «реестры признаков должны превратиться в skill и подключиться, и мы должны выбрать специальный
> рабочий репозиторий, в котором будет запускаться этот агент, сразу получать доступ к этим навыкам…
> на вкладке, где мы подключаем OpenAI, должна быть ещё вкладка подключить сессию Anthropic Agent SDK
> по подписке через браузер»

🛑 **НИЧЕГО ИЗ ЭТОГО НЕ ИЗМЕРЕНО, И СЛЕДУЮЩАЯ СЕССИЯ ОБЯЗАНА ПРОВЕРИТЬ ПЕРВОИСТОЧНИКОМ, А НЕ ПАМЯТЬЮ:**
идёт ли Agent SDK по подписке через браузерную сессию или по ключу с потокенной оплатой · может ли
реестр признаков стать его навыками · нужен ли отдельный рабочий репозиторий.
🔒 **ДЕЙСТВУЮЩИЙ ЗАКОН О `SKILL UPLOADS` СЮДА НЕ ПЕРЕНОСИТСЯ АВТОМАТИЧЕСКИ:** он написан про песочный
контейнер **на стороне провайдера**, не видящий нашего слоя данных, а Agent SDK работает в нашем
процессе. Запрет не действует — но и разрешение не выведено, пока не измерено.

🔒 **OpenAI ОСТАЁТСЯ ПРОЕКТУ В ЛЮБОМ РЕЖИМЕ** — голосовой ввод и граф знаний живут на нём. Переключение
режима не отменяет ключ OpenAI и не делает его необязательным.

### Что уже измерено и не пересматривается без нового замера

- **эмбеддинги годятся для ФОРМЫ и дают ровно случайность для СМЫСЛА** (106-4): `initiator` 100 % ·
  `field` 83 % · `material` 78 % против `intent` 12 % · `entity` 17 % · `destination` 33 % — последние
  три ровно на уровне случайного угадывания. Форма берётся из метаданных бесплатно, смысл идёт сразу
  к классификатору;
- **встроенных признаков 36**, не 25 — измерено вызовом `builtinFacts()`;
- **живого корпуса не существует** — журнал каналов охватывает один день тестирования.

---

## 🔒 ЗАКОНЫ ЭТОГО РЕПОЗИТОРИЯ — РЕШЕНИЯ ВЛАДЕЛЬЦА, ДЕЙСТВУЮЩИЕ ВО ВСЕХ БУДУЩИХ ШАГАХ

### 🪦 Единственное хранилище — ЗАКОН ПЕРЕСТАЛ ИСПОЛНЯТЬСЯ 2026-09-05 (ревизия, шаг 116)

🪦 **Закон был верен, пока разговор шёл через чат `:3600`.** В новой цепочке Telegram → Claude Code
переписка живёт у Anthropic и в Telegram, и наш Postgres её не видит вовсе. Это **выбор владельца, а
не поломка**, и цена названа в блоке «РЕВИЗИЯ 2026-09-05». Слова владельца сохранены дословно, потому
что закон снова станет действующим для всего, что пойдёт ЧЕРЕЗ чат.

> «Технические решения нужно строить только так, чтобы нативные системы хранения сообщений внутри
> чата была бы единственным источником данных. Чтобы никаких отдельных промежуточных слоёв хранения
> данных не возникало в связи с Telegram»

Канал разговора — колонки родной таблицы `Chat` (`channel`, `channelChatId`, `channelWho`,
`channelBot`), не таблица соответствий рядом. Заголовок разговора («Telegram · @имя») человеку, а не
машине: Telegram шлёт по числовому `chat_id`, имя — переменное, разбирать заголовок кодом нельзя.

### 🪦 Telegram — не главный вход — ОТМЕНЕНО 2026-09-05 (ревизия, шаг 116)

🪦 **ПЕРЕВЁРНУТО ПРЯМЫМ РЕШЕНИЕМ ВЛАДЕЛЬЦА.** Действующее: **Telegram — единственный вход к ИИ**.
Ссылки на чат в приложении больше нет — она удалена из подвала той же ревизией. Прежнее слово
владельца сохранено дословно, потому что на нём построены `answer.ts` и серверный путь генерации:
они живы, и их устройство объясняется этой цитатой.

> «Telegram это не контрольная точка входа. Когда к нам приходит новое приложение в нём уже есть
> ссылка на чат, и Telegram здесь только расширение функциональности для редких кейсов; в
> большинстве случаев пользователь будет пользоваться мобильным чатом в вебе»

Браузерный путь генерации (`POST /api/chat`) — основной и не переделывается; серверный путь
(`answer.ts`, `answerInboundMessage`) — обязательный, но побочный: нужен только потому, что у
сообщения из Telegram нет браузера.

### 🔒 Ботов ровно ДВА — уточнено 2026-09-05 (ревизия, шаг 116)

Прежний «вариант A» оставлял открытым, СКОЛЬКО ботов заводить. Владелец закрыл вопрос числом:
**бот №1 — управление агентами и автоматизация** (работает, живёт под pm2), **бот №2 — управление
разработкой** (не начат, не описан, **не центр архитектуры**). Третьего не заводить.

Механика адресации из «варианта A» **остаётся в силе** и ей не противоречит: разговор адресуется
парой «бот × собеседник» (`conversationId`), а не общим пулом. Несколько независимых токенов на одну
учётную запись без привязки к конкретному собеседнику — по-прежнему нет.

Его слово тогда: **«a»**.

### Область размышления — ChainOfThought, не reasoning

`reasoning` AI SDK несёт текст, который порождает МОДЕЛЬ о своём ходе — не годится для наших шагов
(запрос к БД, реестр, инструмент), которые рождает наш код. `ChainOfThought` + Data Parts
(`type:"data-parse-step"` в `Message_v2.parts`) — персистентно, без второй таблицы.

### Построенное согласуется глазами владельца

Зелёные доказательства отвечают на вопрос «работает ли» и не отвечают на «то ли это». Экран, вкладка,
форма — предъявляются построчно после доставки, следующий подшаг открывается только после его слова.

---

## 🔒 ИЗМЕРЕНО — НЕ ИСКАТЬ ЗАНОВО

**Служба каналов `:3500`** (`ai-workspace/services/channels/server.js`):

| Что | Где | Форма |
|---|---|---|
| отправка текста в Telegram | `POST /telegram/send?bot=<id>` | `{chatId, text, parseMode?}` → `{ok, messageId}`; `parseMode` необязателен, белый список — только `"HTML"` |
| отправка файла | `POST /telegram/sendFile?bot=<id>` | `{chatId, base64, kind, name}` → `sendPhoto`/`sendVoice`/`sendDocument` по `kind` |
| куда толкать входящее | `telegram.hookUrl` + `hookSecret` в `config.json` | указывает на `chat.aifa.dev/api/channels/inbound` |
| журнал | `inbox.jsonl`, строка на сообщение, `direction: in\|out` | лимита хранения нет; `out` пишется ТОЛЬКО когда Telegram принял — журнал сам по себе негативный контроль |

**Чат (этот репозиторий):** Postgres + drizzle, семь таблиц (`User`, `Chat`, `Message_v2`,
`Vote_v2`, `Document`, `Suggestion`, `Stream`); запись — `lib/db/queries.ts` (`saveChat`,
`saveMessages`); вход — `lib/fractera/session.ts` → служба `:3001`, своего входа нет по закону
шага 96; файлы — медиатека `:3300` (`lib/fractera/media.ts` → `fetchMedia`/`uploadToMedia`), не
Vercel Blob.

🛑 **ГЛАВНОЕ ОГРАНИЧЕНИЕ ДВИЖКА: ГЕНЕРАЦИЮ ЗАПУСКАЕТ БРАУЗЕР.** `useChat` шлёт `POST /api/chat`, и
ответ рождается в стриме этого запроса — сообщение из Telegram приходит, когда браузера нет вовсе.

🛑 **СЛУЖБА ТОЛКАЕТ ВХОДЯЩЕЕ РОВНО ОДНОМУ АДРЕСУ.** Второй адрес хука не существует — переключение
`hookUrl` отключает разбор запроса в `fractera-next-starter`, если он там ещё раз понадобится.

🛑 **`Streamdown` (паспорт, `fractera-next-starter`) санирует HTML своей схемой поверх результата
`remarkPlugins`.** Тег, которого нет в `hast-util-sanitize`'s `defaultSchema` (например `u`),
пропадает молча, даже если плагин верно его построил. Лечение — `allowedTags={{tag: []}}`
у `<Streamdown>`, штатный параметр, а не обход.

🛑 **Сайдбар чата — ненадёжный источник правды для «что удалено/сохранено».** Тост об успехе не
значит, что клиентский список ревалидировался. Проверка — прямой SQL или та же дверь API
(`fetch('/api/chat?id=...', {method:'DELETE'})`), не текст на экране.

---

## Что осталось в `fractera-next-starter` — ПРИОСТАНОВЛЕНО, а не отменено

Шаг 91 «Разбор запроса»: закрыты 91-1 … 91-5, **не начаты 91-6 … 91-9**. Долги 81-11, 81-12.
Документ 94 (внешний исполнитель) остановлен словом владельца 2026-09-02. План 95 не начат.

**Два дефекта, найденных 2026-09-03 и не закрытых:**

1. `lib/facts/builtin.ts` объявляет каждому инициатору адрес хранения `tgdesk_messages.initiator`,
   **а такой колонки нет** — ни в `SCHEMA`, ни в `LATE_COLUMNS`. Реестр показывает человеку адрес,
   которого не существует.
2. Якорь, собранный из русского заголовка, не пересылается и ломается при смене языка — адреса и
   якоря должны быть латинскими, языковой префикс `ru`/`en` достаточен (замечание владельца).

---

## Состояние репозиториев на конец дня 2026-09-03 — сверено по всем задетым

| Репозиторий | `main` = `origin/main` | Замечание |
|---|---|---|
| `fractera-chatbot-starter` | см. `git log -1` | дом кода и учёта |
| `ai-workspace` | см. `git log -1` | служба `:3500` |
| `fractera-next-starter` | см. `git log -1` | экран бота, паспорт; ветка `new-step-98` переименована в `old-step-98` при закрытии |
| `fractera-easy-starter` | `8c0a85c` | не задет сегодня |
| `/code` | — | остаётся домом ФЕДЕРАЛЬНОГО учёта платформы, не закрывается |

## 🔒 Сервер для сборки — слово владельца 2026-09-03

`213.199.61.7`, домен `aifa.dev`. Root-пароль назван владельцем в разговоре и живёт в федеральном
`CLAUDE.md`, а не здесь: пароль в файле репозитория, который отправляется на GitHub, — это утечка,
а не удобство.
