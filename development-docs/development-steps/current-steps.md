# Состояние работы — служба Telegram (порт 3600)

> **Читается ПЕРВЫМ в каждой сессии.**

## Где мы сейчас

**Открытых шагов нет.** 2026-09-10 владелец закрыл всё словом: «наша задача закрыть все ранее
открытые шаги потому что мы начинаем работать над памятью с чистого листа мы подготовили проект».

- планы построенных шагов (106, 133, 138, 141, 147, 148, 155, 156, 164, 171, 175, 177, 181)
  **удалены** — их работа закрыта итогами в `completed-steps/`, сами планы восстановимы из git;
- планы, до которых работа не дошла (139, 140, 142, 149–154), **уехали в архив**
  `archive/closed-2026-09-10/` и названы строками в `development-docs/BACKLOG.md`: архив при старте
  не читается, и без указателя они исчезли бы;
- шаг **181 закрыт целиком**: итог `completed-steps/181-main.md`, репорт фичи
  `reports/feature-subscription-and-keys-became-their-own-entrances-in-both-services.md`.

🔒 **РАБОТА ПО ПАМЯТИ ВЕДЁТСЯ НЕ ЗДЕСЬ.** Её учёт переехал в собственный репозиторий:
`fractera-memory-starter/development-docs/development-steps/current-steps.md`. Шаги 175–181 остались
здесь историей — это тот период, когда память ещё не имела своего учёта.

## Что осталось незакрытым и названо вслух

- **177-5 не начат** — тот же журнал у агента Telegram; строка в `BACKLOG.md`;
- **бот молчит:** сессия `fractera-agent-channel` держит на экране `organization changed on this
  machine — run /remote-control … or /login`, последний ответ модели 2026-09-09 23:24. Лечится
  перезапуском, но перезапуск обрывает живую сессию с её контекстом — **решение за владельцем**;
- **проверка «опрашиватель Telegram ровно один» устарела** в федеральном `CLAUDE.md`:
  `pgrep -fa "bun run --cwd"` даёт `0` при живом канале, плагин работает внутри `claude --channels`;
- вид трёх поверхностей чата (вкладка подключения, вкладка OpenAI, страница подписки) владельцем ещё
  не подтверждён.

## Дорогие измерения — не искать заново

- порт 3600 после `pm2 reload` держит **потомок** pm2 (`node server.mjs` ← `sh -c` ← `pnpm` ← pm2):
  сверять принадлежность дереву, а не равенство pid;
- `/v1/*` памяти проверяет заголовок `x-data-secret` со значением `DATA_SECRET`;
- расширение Chrome в сессиях агента блокирует скрипт на страницах с сессией и переход на
  `chat.aifa.dev` — вид подтверждает владелец.

## Следующее действие

Ждать слова владельца по боту и по трём поверхностям. Новая работа идёт по памяти, в её репозитории.
