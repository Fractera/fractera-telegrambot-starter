# ОБНОВЛЕНИЕ ПЛАНА 144 — 2026-09-08, ИЗМЕРЕНИЕ ВМЕСТО ПРЕДПОЛОЖЕНИЙ

**Слово владельца 2026-09-08:** «go to finish non stop» — второй раз за день. Этап 9 не проводится,
план дописан и исполняется.

## Что уже построено (измерено, а не вспомнено)

| Из плана 2026-09-07 | Состояние на 2026-09-08 |
|---|---|
| 144-1 колонки лестницей | ✅ **закрыт**, итог есть; семь поздних колонок живут |
| 144-2 саммари при закрытии | ✅ пишется дверью `agent/close` (143) |
| 144-3 теги — ключи реестра | ✅ пишутся из ленты прогона (143-4), свободных слов нет |
| 144-6 экран на настоящих данных | ✅ `queryAutomationsLive`, три исхода `db`/`empty`/`down` |

## 🛑 ЧТО ИЗМЕРЕНИЕ НАШЛО ВМЕСТО ЭТОГО — ТРИ ЛЖИВЫХ ПОЛЯ НА ЭКРАНЕ

`app/[lang]/settings/_lib/automations.ts` → `fromRow()`:

| Поле | Что стоит | Почему это ложь |
|---|---|---|
| `calendar` | **всегда `false`** | у автоматизации есть строки `schedule_entries` — напоминания и ступени `chain`. Отбор «только с календарём» не находит НИЧЕГО и выглядит исправным |
| `map` | `Boolean(scopeKey)` | 🛑 **охват — не геометка**, и §3к предупреждает ровно об этой путанице. Разговор с названным городом помечается «есть метка на карте», хотя никакой метки нет |
| `status` | `confirmState` (`draft`/`confirmed`) | состояние РАБОТЫ (`open`/`step-closed`/`closed`) построено в 143-1 и на экран не доехало |
| `steps` | **всегда `0`** | лента прогона существует с 143-3 |

🔒 **ЭТО НЕ «НЕДОДЕЛКА», А ИМЕННО ЛОЖЬ: поля показывают уверенное значение там, где данных не
брали.** Пустое поле человек прочтёт как «пока нет»; `false` он прочтёт как «проверено — нет».

## Подшаги (новая редакция)

| Подшаг | Результат одной строкой |
|---|---|
| 144-2 | состояние работы из 143-1 доезжает до экрана; отбор по нему настоящий |
| 144-3 | календарь перестаёт лгать: есть строка расписания — есть отметка |
| 144-4 | геометка отделена от охвата; охват показывается как охват |
| 144-5 | вердикты человека тремя состояниями, «неизвестно» отличимо от «не нравится» |
| 144-6 | число строк прогона вместо нуля; отбор и счёт — чистым SQL, без модели |

## Чего в шаг НЕ входит

Указатель `automation_facts` (145) · вектор и его измерение (146) · публичная страница (142).
