# Шаг 160 — стандарт памяти: документ, вкладка, приведение архитектуры

**Заведён:** 2026-09-08. **Репозиторий:** `fractera-telegrambot-starter`.
**План утверждён владельцем** через интерфейс выбора; полный текст —
`~/.claude/plans/groovy-waddling-jellyfish.md`.

## Откуда взялся — слова владельца дословно (2026-09-08)

> «Прежде чем мы продолжим, нужно чётко установить правила сторожа: не пропускать в личную таблицу
> данные с глубиной больше единицы».

> «Вопрос глубины рассуждения — это ещё один параметр, который должен быть динамическим… важно, чтобы
> управление погружением было подтверждено пользователем, ибо углубление на каждый уровень — это
> рекурсивное усложнение задачи».

> «Нам нужно создать отдельный документ, как работает память. Markdown, и детально описать всё, что
> мы здесь стандартизируем, а потом к этому стандарту привести архитектуру».

## Решения владельца, принятые по ходу

| Вопрос | Ответ | Дата |
|---|---|---|
| куда пишется углублённое саммари | **вектор сразу, граф — только после подтверждения человеком** | 2026-09-08 |
| что стирать в базе перед тестом | работа + память о нём; сообщения и медиатека сохранены | 2026-09-08 |
| собирать ли службу | да, после явного разрешения | 2026-09-08 |

## Подшаги

| Подшаг | Результат одной строкой | Состояние |
|---|---|---|
| **160-1** | документ `MEMORY-STANDARD.md`: три глубины, четыре уровня, кейсы по всем слоям, критический анализ | **готов**, `4549a28` |
| **160-2** | вкладка «Как работает память» и таблица знаний с поиском и пагинацией | **готов**, `3d6f272` |
| 160-3 | `valueType: "object"` и хранение объектом вместо прозы | не начат |
| 160-4 | сторож глубины на записи: три проверки, отказ говорит словами | не начат |
| 160-5 | `remember_many` — свободный рассказ одним вызовом | не начат |
| 160-6 | якорь сущности у записей графа | не начат |

🔒 **СТОРОЖ ГЛУБИНЫ ИДЁТ РАНЬШЕ `remember_many`**: массовая запись без сторожа — способ занести
второй порядок в таблицу быстрее, чем поштучно.

## Что измерено до постройки

| Слой | Состояние | Цена |
|---|---|---|
| личная таблица | 14 признаков о человеке, 49 всего | 33–227 мс |
| вектор | коллекция `tgdesk`, 8 записей | ~50 мс |
| граф LightRAG | 8 документов, три vdb-базы внутри | **10 864 мс** |
| второй слой реестра | `aggregate` 14 · `subject` 14 · `scope` 1 · **шесть свойств — нули** | — |

## Чего в шаг НЕ входит

Автоматическое углубление без спроса · запись догадок в граф · наполнение второго слоя реестра ·
оживление заглушек `calendar`/`geo`/`links` в «Логах».
