# 142-7 — законы записаны, паспорт сверен с построенным

## Что строится
- `/code/CLAUDE.md` и `CLAUDE.md` службы: четвёртый слой, публичный корень, три закона §3д.
- `development-docs/PASSPORT.md` §3д правится по факту построенного, а не остаётся замыслом.
- Число слоёв автоматизации исправляется ВМЕСТЕ с их перечислением (закон 76-6).

## Что считается готовым
Новая сессия, прочитав инструкции, строит публичную автоматизацию, не изобретая второй способ.

## Чем доказывается
1. **Текст:** грепом — ни одного места, где слоёв названо меньше, чем перечислено.
2. **Холодная проверка:** описанный порядок повторяется по инструкции и даёт публичную страницу;
   расхождение инструкции с кодом правится в инструкции.

## На что опирается
142-6.

## Чего в подшаг не входит
Публичная новость и документация витрины — только с отдельного одобрения владельца.
